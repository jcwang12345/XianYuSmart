# PUB-01～03 商品发布硬验收基线

本文件细化 `XIANYU_MATRIX_PRODUCT_REQUIREMENTS.md` 的 PUB-01～PUB-03。未通过的能力必须显示为未验证、不可用或安全降级，不得用静态入口、默认值或本地成功冒充平台实现。

## 安全边界

- 生产店铺不得执行发布、删除或重复提交等破坏性验收。
- 隔离发布仅允许 `qa` profile、显式开关、Tenant-A、账号 `101/102/103`、`QA_LOCAL` 通道和 `QA-PUBLISH-` 标题前缀同时满足；响应和数据库必须明确 `QA_FIXTURE/QA_MOCK`、`platformNetworkCalls=false`、`platformWrite=NOT_PERFORMED`。
- 官方授权、鱼小铺/个人号能力必须来自账号通道证据；无证据时为 `UNKNOWN/NOT_VERIFIED/UNAVAILABLE`。

## PUB-01 多通道发布（P0）

- P01-01：账号切换后只列出该租户、该账号真实存在的通道。
- P01-02：不可用通道显示连接、授权、过期、来源、覆盖度、最近核验和失败原因，不能选择。
- P01-03：没有官方授权记录时可展示“未接入”说明，但不得进入可用通道列表。
- P01-04：能力矩阵覆盖发布、类目、编辑、SKU、小刀、同步和授权；无探测证据的 SKU 不得显示 READY。
- P01-05：`QA_LOCAL` 只有安全隔离开关启用时才可用；生产 profile 即使数据库存在记录也不可用。
- P01-06：前后端发布权限统一使用商品菜单与商品写权限，越权返回 403。
- P01-07：跨租户账号 ID 猜测返回 403/404，不能泄露通道或账号信息。
- P01-08：通道能力刷新、加载、错误、空状态均保留当前账号上下文。

## PUB-02 发布表单与预检（P0）

- P02-01：实物/虚拟类型、标题、描述、售价、库存、交付方式和至少一张图片为可校验字段。
- P02-02：售价大于 0 且最多两位小数；库存为正整数；图片 1～9 张且仅 HTTPS 或本地媒体地址。
- P02-03：实物不可线上交付，虚拟商品不可快递；非包邮快递必须有运费模板。
- P02-04：违规词返回具体命中项；视频、多 SKU 未验证时明确阻止，不能静默丢字段。
- P02-05：预检返回通道能力、内容策略、类目、地址、图片数、请求 ID 与载荷指纹。
- P02-06：真实通道预检使用最终生产请求构造器；QA 隔离预检必须明确 `previewUsesProductionBuilder=false`。
- P02-07：表单任一业务字段变化后原预检失效，并生成新请求 ID。
- P02-08：叶子类目/行业、原价、发货地、成色、outerId、类目属性和 SKU 只有在适配器证实支持后才能提交；当前缺口必须展示安全降级。
- P02-09：图片支持排序、删除和素材复用；空态、上传中、失败和重试可见。
- P02-10：桌面、1080P、4K 和手机窄屏无横向溢出，底部动作不遮挡内容。

## PUB-03 结果、幂等与补偿（P0）

- P03-01：执行先创建持久化素材和发布任务，任务中心可按任务 ID、请求 ID、账号查看。
- P03-02：同一请求 ID、同一业务载荷重放返回同一任务并标记 `idempotentReplay=true`。
- P03-03：同一请求 ID、不同业务载荷返回 409；并发冲突只保留一个任务且清理输家孤立素材。
- P03-04：业务载荷指纹忽略 requestId、dryRun、旧指纹和 Map 排序，并规范化数值。
- P03-05：平台失败、结果未知、平台成功但本地落库失败必须是不同状态和恢复建议。
- P03-06：UNKNOWN 的最大尝试次数为 1，不自动重发；可按请求 ID 查询当前状态。
- P03-07：发布成功生成商品主档、商品事件和统一审计；事件/审计含账号、操作者、请求 ID、通道、结果层和数据来源。
- P03-08：本地待修复状态保留商品 ID，并明确禁止真实通道重复发布。
- P03-09：QA SUCCESS、LOCAL_PENDING、UNKNOWN 三种场景可回放，且网络与平台写调用均为 0。
- P03-10：能力未验证、权限撤销、账号失效和通道变化在执行前再次阻止。
- P03-11：结果页显示请求 ID、任务 ID、商品 ID、通道、结果状态、恢复建议和查询入口。
- P03-12：不得把任务排队、本地落库或 QA Mock 显示为真实平台发布成功。

## 当前明确降级项

- 真实 PC 发布适配器目前只验证单 SKU；多 SKU 为 `NOT_VERIFIED`。
- 叶子类目/行业、原价、成色、outerId、完整类目属性和视频发布尚无稳定平台适配证据，不宣称实现。
- `QA_LOCAL` 只用于隔离验收，不代表闲鱼平台能力。
