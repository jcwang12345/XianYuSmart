# XianYuSmart V4 长期目标与合并实施计划

版本：4.0
生效日期：2026-09-15
需求基线：`XIANYU_MATRIX_V4_PRODUCT_REQUIREMENTS.md`
现场证据：`XIANYU_MATRIX_V4_FUNCTION_AUDIT_2026-09-15.md`

## 1. 长期目标

在现有 XianYuSmart 仓库上持续合并开发，形成一套能稳定管理 20～30 个闲鱼账号的私有化矩阵系统。系统以真实业务状态为核心：每个账号独立运行，商品、订单、客服、通知和审计共享一致事实；操作者能从经营异常进入具体对象、完成安全处置，并能解释每一次自动化为什么发生、是否成功以及如何恢复。

当前已经存在的长期目标“把鱼麦多的更好功能都移植”不另行替换；从本文件生效后，该目标按 V4 PRD 和 V4 全功能审查执行。只采纳第三方成熟的产品方法，不复制品牌、文案、素材，也不把未验证平台能力包装为已实现。

## 2. 合并策略

1. 保留当前工作树、数据库迁移和已通过的实现，不重写已验证模块。
2. 一次提交只覆盖一个可回归主题；后端真值修复与大规模导航重构分开。
3. 所有批次按“数据模型→API→权限→审计→故障注入→核心测试→前端→Product Design QA→独立测试”推进。
4. 兼容页先读同一数据源，再做重定向，最后删除；过渡期不得继续显示冲突的 0 数据。
5. 每批最终源码重新执行前端生产构建，检查 `src/main/resources/static` 哈希资产，不采用测试任务生成的中间产物。
6. MySQL 继续复用 Docker；Java/Node 编译与应用运行使用 macOS 裸机，测试站固定 `http://127.0.0.1:3000`。
7. 平台破坏性动作只走 QA Mock；生产店铺只读。

## 3. 当前基线与先决条件

- 当前代码：`HEAD 7a87b7a`（`v2.7.0`），工作树包含 `v2.7.1` 候选修改。
- 当前正在收口：`XYM-IM-001` AI 接管幂等回放、`XYM-IM-002` 知识版本/生效/过期；`XYM-IM-003` 页签语义为 P2 相邻项。
- 现有 V44/V45 迁移、`GoodsKnowledgeService`、AI 接管服务修改必须先审查和验证，再形成独立提交。
- 独立测试任务：`01a09b7a-ce74-79f2-bca4-92794cb8dfea`；只有本批功能、设计和 QA 门禁齐全后才发送正式交接。

## 4. 实施波次

### Wave 0：候选工作树收口与全局 P0（v2.7.1）

范围：IM-02～06、BASE-01/02/06/11/12、PRD-03、现有独立测试缺陷。

交付：

- 修复 AI 接管同 requestId 重放标记和单事件/单审计语义；
- 完成知识草稿、生效、过期、停用、版本追溯和回复引用；
- 修复知识资料查询失败，并提供持续错误、一键重试和最后成功时间；
- 商品 SKU 主档声明与子项真值一致；缺同步子项时禁用履约和 SKU 动作；
- 路由切换滚到顶部，详情返回恢复来源上下文，状态刷新不劫持滚轮；
- 首帧不闪 0/空态；修复能力矩阵稳定系统异常；
- 补 AI 页签语义、弹窗关闭名称、Esc 和基础焦点测试。

退出门禁：现有 IM P1 为 0；相关单测/API/DB/E2E 通过；迁移能在 MySQL 5.7 从旧版本升级；最终静态资产重建；独立测试回归通过。

### Wave 1：信息架构和统一体验底座（v2.8.0）

范围：BASE-01～14、DASH-01～03、TASK-01。

交付：

- 经营罗盘成为唯一经营总览；数据看板合入“实时服务”页签；
- 驾驶舱只承担统一待办和跨域异常，不重复经营数字；
- 商品任务中心进入主导航；统一任务卡、结果、证据、导出和审计入口；
- 建立统一 `DataEvidence`、`AsyncState`、`CapabilityGuard`、`BusinessEvent`、`FullscreenDetail`、`SafeDialog` 组件；
- 建立全站路由滚动/焦点策略、中文业务事件映射和错误恢复模式；
- `/automation`、`/pending-orders`、`/qrlogin`、`/operations` 重复入口先重定向/迁移提示。

退出门禁：主导航不出现重复事实；兼容路由不显示冲突数据；七类状态和五种视口完成证据；旧书签可达新入口。

### Wave 2：账号资产、连接与风险（v2.9.0）

范围：ACC-01～10、NTF-03、SEC-01。

交付：

- 接入事务、重复防护、敏感凭证只写、连接状态单一来源；
- 账号360固定为居中近全屏，完成画像/指标/信用/处罚与证据下钻；
- 新接入和续期共用二维码组件，显示账号ID、备注、店铺名和最长安全倒计时；
- 凭证过期、掉线、验证、处罚进入统一风险待办；
- 每账号运行档案、凭证、队列、限速与指纹冲突证据可查；代理字段保留但不启用；
- 分组、跨页选择和批量账号任务复用持久化任务底座。

退出门禁：三个 QA 店铺并发隔离可证；跨租户/越权用例通过；二维码过期、刷新、确认和失败完整；桌面/手机账号360完成 QA。

### Wave 3：商品经营和发布闭环（v3.0.0）

范围：PRD-01～06、PUB-01～07、TASK-01，以及 P01-01～P04-22、PUB 专项全部条款。

交付：

- 商品矩阵六状态、筛选快照、1000商品性能和保存视图；
- 商品360五页签真实接口、SKU一致性、字段diff和营销安全降级；
- 完整实物/虚拟发布：身份、行业/叶子类目、动态属性、成色、价格优惠、库存、位置物流、媒体、SKU、履约和售后；
- 右侧买家视角预览与最终发布结构同源；
- 草稿版本、同源预检、发布状态机、平台回读、改写diff和安全补偿；
- 商品任务 100/1000 项、部分成功、未知、取消、重启恢复、失败项重试、通知和审计；
- 推广、曝光、佣金和营销只在适配能力证据充分时开放。

退出门禁：P01-01～P04-22 与 PUB 全通过；0/1/50 SKU 回归；价格 0.001/1.234/两位小数/上限校验；无生产写入。

### Wave 4：订单、履约、退款和通知闭环（v3.1.0）

范围：ORD-01～09、NTF-01～05、卡密、固定模板、自动发货。

交付：

- 跨店订单驾驶舱和订单360五页签，消息空态连接真实会话；
- 卡密预占/消费事务、发送幂等、断网恢复、未知与人工核对；
- 已发货终态和业务事件去重，彻底阻止网络重连后重复推送待发货；
- 退款、退货、补发、争议事实和截止时间完整；平台决策按能力安全降级；
- 确认收货后的评价邀请、自动评价、追评、频控和人工替代待办；
- 统一通知事件、渠道、范围、站内处理和发送记录；续期二维码直接进入推送；
- 待发货兼容页迁入统一履约工作台并退场。

退出门禁：断网、重复回调、重启、卡密不足、部分同步、结果未知、重复通知和退款权限回归通过；外部渠道使用隔离 Mock。

### Wave 5：客服、知识与买家经营（v3.2.0）

范围：IM-01～08、CRM-01、AI 策略与知识。

交付：

- 会话与业务通知双收件箱数据、计数和店铺上下文同源；
- 文本/图片/商品卡可靠发送和人工接管完整生命周期；
- 知识版本、有效期、商品/店铺范围和回复引用可追溯；
- 内置软件商品知识模板：7天首次激活、Office/WPS插件、视频教程、售后群二维码、人工客服；
- AI 回复显示命中内容、版本、模型、置信、耗时、决策和送达结果；
- 策略优先级、冲突检测、测试台、记录和回滚；
- 手机客服采用列表→对话→上下文，买家360与会话/订单双向返回。

退出门禁：未命中、低置信、敏感、超时、重复请求和 AI 不可用 E2E 通过；知识过期不会继续命中；390px 完成核心客服处置。

### Wave 6：增长工具和系统治理（v3.3.0）

范围：OPS-01～05、SEC-02/03、AUD-01/02、SYS-01～03。

交付：

- 素材、商机、比价、货源和工作流全部补来源、版本、许可、运行实例与审计；
- 无可靠来源的分销结算隐藏，不维持假空表；
- 成员权限模板、差异预览、即时撤销和敏感动作独立授权；
- 2FA 按后端挑战显示第三框，恢复码/设备/限速完整；
- AI、SMTP、Webhook 等密钥只写不回显，连接测试脱敏；
- 审计业务化摘要、字段diff、查询导出和原始快照权限；
- 备份导入 dry-run、版本/租户/冲突预检、恢复点和强确认。

退出门禁：跨租户、越权、权限撤销、密钥回显、备份冲突、工作流补偿和大数据审计通过；所有系统页完成桌面/手机设计 QA。

## 5. 每批固定产物

每个批次在 `XIANYU_MATRIX_IMPLEMENTATION_LOG.md` 追加：

- 需求编号与验收用例；
- 变更文件、数据库迁移、API 和兼容策略；
- 权限、审计、幂等、异常态和安全降级；
- 类型检查、生产构建、后端单测/集成测试、迁移验证的命令与结果；
- 390×844、1366×768、1920×1080、3840×2160、200% 以及七类状态的 Product Design QA 证据；
- 残余降级、已知限制和独立测试缺陷闭环。

## 6. 分支、提交与版本规则

- 提交信息以需求号开头，例如 `fix(IM-03): ...`、`feat(PRD-05): ...`；
- 迁移只新增，不修改已发布迁移；MySQL 5.7 兼容；
- 每个可测试批次先形成候选提交，再由独立测试回归；缺陷按 `XYM-*` 编号修复；
- 只有 P0/P1 关闭、构建和迁移证据齐全、最终静态资产干净，才升级版本和打 tag；
- 不提交账号 Cookie、真实密码、二维码会话、平台回执敏感原文、数据库数据目录或 JVM 崩溃转储。

## 7. 独立测试交接模板

交接给任务 `01a09b7a-ce74-79f2-bca4-92794cb8dfea` 时必须一次提供：

1. 功能开发、Product Design 前端开发、设计 QA 三项完成声明；
2. commit/tag、迁移清单、变更摘要和残余安全降级；
3. macOS 裸机启动命令、MySQL Docker 地址、前后端 URL；
4. QA 账号/租户/角色、店铺 101/102/103、安全对象范围；
5. Mock/故障注入、重放、断网、重启、权限撤销和大数据夹具方法；
6. 类型检查、生产构建、后端测试和迁移验证命令及结果；
7. 明确禁止生产发布、退款、删除、申诉、发货、实际消息和外部通知。

收到缺陷后按编号修复，在实现日志记录根因、修改、测试和数据恢复，再通知原任务回归；不得用重新生成夹具掩盖缺陷。

## 8. 总完成定义

V4 长期目标只有在以下条件同时成立时才能标记完成：

- V4 PRD 的 P0/P1 无未决缺陷；
- 重复入口完成迁移，所有主入口使用同一事实源；
- 账号、商品、订单、客服和通知五条核心链路能够在 QA 中断网、重启、重复请求后恢复；
- 未同步不显示0，平台不可用不显示成功，结果未知不自动重试；
- 五种视口和七类状态完成设计 QA；
- 独立测试完成全量验收，代码推送并打正式版本号。
