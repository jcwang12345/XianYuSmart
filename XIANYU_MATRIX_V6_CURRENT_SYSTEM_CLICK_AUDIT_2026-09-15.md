# XianYuSmart V6 当前系统逐功能点击与截图基线

版本：6.4-audit

取证日期：2026-09-15

运行环境：macOS 裸机应用 `http://127.0.0.1:3000`，Docker MySQL 5.7，Tenant-A，OWNER 角色
代码基线：`8cd2f6083a44aed66fcc30ed06998ba109454456` / `v3.0.0-rc.1`

## 1. 目的与证据规则

本文件把“菜单里有入口”“页面能打开”“后端真实可用”“平台能力已验证”分开记录，作为后续需求、开发和独立验收的共同事实源。

本轮使用 Product Design 审计工作流，在真实本地界面逐页导航、点击安全入口并截图。截图已在本任务中按证据编号生成；浏览器插件返回的是会话内图像字节，未伪造本地图片路径。任何未进入最终写入步骤的表单，仅证明界面和前置校验存在，不证明保存或平台写入成功。

能力等级：

- `L0 路由存在`：只有路由或迁移说明。
- `L1 页面可见`：稳定显示结构、字段和空态。
- `L2 本地闭环`：API、数据库、权限、审计和回读可验证。
- `L3 隔离执行`：QA Mock 可执行完整状态机。
- `L4 平台确认`：真实适配器有回执和回读；本轮不执行生产写验证。

安全边界：未执行真实发布、上下架、改价、库存、删除、发货、退款、申诉、外部消息、成员保存或外部通知；仅使用只读页面、未提交表单和 QA 隔离数据。

## 2. 主入口点击与截图清单

| 证据 | 入口 | 已点击/已观察能力 | 当前等级 | 当前结论 |
|---|---|---|---:|---|
| PD-001 | `/dashboard` 经营罗盘 | 店铺/分组、1/7/30 天、金额/订单/买家/退款/曝光/咨询、趋势、漏斗、异常、排行 | L2 | 数据证据和“未知不为 0”表达较完整；首屏刷新期间仍会先显示占位，需验证最后成功值保留 |
| PD-002 | `/command-center` 运营驾驶舱 | 账号总览、异常任务、客服队列、能力矩阵 | L1 | 页签和摘要存在；取证时长期停留刷新中，未证明数字与详情同快照 |
| PD-003 | `/accounts` 账号矩阵 | 分组、搜索、连接/风险筛选、三店列表、店铺详情 | L2 | 店铺事实和证据来源可见；列表首次截图早于异步稳定态，稳定后为 3 店 |
| PD-004 | 账号“新建分组” | 名称、说明、包含店铺、取消/保存 | L1 | 表单清晰；本轮不保存，需用 Mock 验证重复名、店铺范围、审计和撤权 |
| PD-005 | 账号“手动接入” | 账号备注、Cookie、取消/添加 | L1 | 只有备注和 Cookie；缺查重结果、凭据来源、风险说明、只写不回显状态和接入预检摘要 |
| PD-006 | 账号“扫码接入” | 生成真实二维码、等待扫码、会话 ID | L2 | 能生成二维码；未显示账号 ID、账号备注、生成/失效时间、剩余倒计时和刷新动作，不满足既定续期需求 |
| PD-007 | 店铺 360 | 店铺画像、指标、信用、通道、运行档案、数据证据、风险、数据看板、编辑备注、管理连接 | L2 | 全屏详情与参考信息架构基本一致；已知 0 与未知 `—` 能区分；平台处罚实时推送仍为安全降级 |
| PD-008 | `/connection` 连接管理 | 三账号列表、连接/WS 状态、详情区域 | L1 | 列表可见；点击账号卡没有切换详情，直接 `/connection/101` 为空白，属于入口断裂 |
| PD-009 | `/goods` 商品矩阵 | 店铺/分组/来源/通道/时间筛选、六状态、343 件、经营摘要、证据、分页、详情 | L2 | 单店上下文稳定后数据可用；首次进入会较长时间显示 0/正在更新，需修加载态和请求竞态 |
| PD-010 | 商品保存筛选 | 名称、保存/取消 | L1 | 仅证明表单；需验证筛选快照、租户/账号范围、重名、删除和审计 |
| PD-011 | 商品批量操作 | 当前页 20 件、跨页最多 1000、同步/擦亮/上下架/改价/库存/删除、每店限速、预检、创建任务 | L3 | 预检结构完整；本轮未创建任务。需继续覆盖 100/1000、部分成功、未知、权限撤销、重启恢复 |
| PD-012 | 商品 360 | 基本、SKU、营销、数据罗盘、事件；本地资料和单品动作 | L2 | 4/4 SKU 和履约映射真实可读；平台写动作按能力降级；200% 等效视口页签视觉丢失 |
| PD-013 | `/product-publish` 发布工作台 | 店铺/身份、实物/虚拟、行业/叶子类目、属性、媒体、价格库存、SKU、物流、服务承诺、预检、买家预览 | L2/L3 | 页面骨架成熟，QA_LOCAL 可隔离执行；官方授权未接入时不可选择，表达诚实 |
| PD-014 | 发布 50 SKU | 2 维、10×5=50 组合、价格/库存/编码表、右侧 `+47` 预览 | L2 | 50 行真实生成；缺批量填充、粘贴导入、行错误聚合和大表效率工具；默认 0 元必须被字段预检阻断 |
| PD-015 | `/product-tasks` 商品任务 | 状态/动作/店铺/操作者/时间筛选、主子任务详情 | L2 | 页面存在；当前默认范围无任务，但商品页显示最近任务，两个入口的默认范围与解释不一致 |
| PD-016 | `/operations` 运营中心 | 使用向导、资源与规则、任务记录、分销结算；素材/地址/采集/规则/返佣/公告/反馈/风控 | L1 | 统计可读取 18 个素材；点击顶部页签后重新加载但仍停留“使用向导”，页签交互未闭环 |
| PD-017 | `/opportunities` 商机发掘 | 搜索/店铺采集、捕获→改写→配置→发布四步 | L1 | 流程和空态可见；未输入外部关键词，尚未证明搜索来源、去重、采集证据和发布交接 |
| PD-018 | `/price-comparison` 全站比价 | 关键词、价格区间、排序、重置 | L1 | 搜索表单可见；尚未证明平台来源、采集时间、样本量、分页和失败降级 |
| PD-019 | `/supplies` 货源库 | 搜索、新增、刷新 | L1 | 空态可见；尚未证明来源许可、有效期、素材完整度、库存/价格新鲜度和转发布映射 |
| PD-020 | `/workflows` 工作流 | 新建、保存、运行、账号、五种节点、最近节点结果、执行记录 | L1 | 画布和入口可见；无已保存实例，未证明版本、连线校验、逐节点幂等、补偿和取消 |
| PD-021 | `/kami-config` 卡密仓库 | 账号范围、新建密钥仓库、配置详情区 | L1 | 当前店铺无配置；需覆盖私有/共享、内部/外部、版本、原子预占/消费/释放和低库存 |
| PD-022 | `/fixed-delivery-templates` 固定内容模板 | 账号选择、新建、列表 | L1 | 空态可见；需覆盖变量、预览、版本、引用影响、多账号检索和审计 |
| PD-023 | `/auto-delivery` 自动发货 | 商品列表、固定内容/卡密二选一、SKU 同步、发货凭证/私聊、图片、确认收货后评价引导 | L2 | 配置面较完整；平台连接不可用时规格同步安全失败；需继续用 Mock 验证防重、未知、重启和 SKU 门禁 |
| PD-024 | `/orders` 订单与售后 | 店铺/履约/退款/日期筛选、金额、覆盖、详情 | L1/L2 | 详情能力源码和既有 QA 可验证；本轮列表跨店与单店均出现刷新不结束或 0 条，深链/列表事实需统一 |
| PD-025 | `/messages` 集成客服 | 买家会话、通知、AI 待接管；三栏、筛选、接管、同步历史、发送、会话资料、黑名单、买家/订单互链 | L2/L3 | 买家会话真实展示；点击通知/AI 页签后账号变成“暂无可用账号”且计数归零，页签范围状态存在回归 |
| PD-026 | `/buyers` 买家管理/360 | 店铺、自动化状态、搜索、订单/会话/商品/评价 | L1/L2 | 既有深链模型存在；本轮列表持续加载，深链也被列表请求阻塞；独立 QA 另发现黑名单链路缺失、金额和时间格式错误 |
| PD-027 | `/auto-reply` 自动回复 | 店铺、商品侧栏、商品配置 | L1/L2 | API 可返回商品，但当前首屏长时间只有空详情；需验证选择恢复、知识版本、关键词、测试台和 AI 降级 |
| PD-028 | `/operation-log` 操作日志 | 店铺、类型、模块、状态、结果层、操作者、请求 ID、时间、导出、分页 | L2 | 查询维度完整；异步加载期间只显示页码，需保留上次成功数据并提供错误/最后成功时间 |
| PD-029 | `/operations-health` 通知与诊断 | 系统检查、异常待办、站内通知、通知渠道、发送记录 | L1/L2 | 入口齐全；点击各页签后仍停留系统检查，主体为空，不能按“已完成五页签”验收 |
| PD-030 | `/settings` 系统设置 | 系统账号、菜单、AI 服务、AI 客服、邮箱、备份恢复、关于；2FA、设备会话 | L1/L2 | 系统账号可见；点击侧栏其他项后高亮变化但内容仍停留系统账号/加载中，设置导航失效 |
| PD-031 | `/admin/users` 团队与权限 | 团队账号摘要、创建账号、刷新 | L1/L2 | 创建入口存在；本轮持续“正在读取全站账号”，未证明成员列表、角色 diff 和即时撤权 |
| PD-032 | `/qrlogin` 兼容入口 | 跳转账号管理、连接管理 | L0 | 正确作为迁移说明，不维护第二套扫码流程 |
| PD-033 | `/data-panel` 兼容入口 | 跳转经营罗盘两个页签 | L0 | 正确退场，不单独取数 |
| PD-034 | `/automation` 兼容入口 | 跳转工作流、发货、回复、任务、诊断 | L0 | 正确退场，不再执行动作 |
| PD-035 | `/pending-orders` 兼容入口 | 跳转待发货筛选、自动发货、履约异常 | L0 | 正确退场，避免旧轮询绕过订单状态机 |

## 3. 响应式与可访问性证据

| 证据 | 场景 | 结果 |
|---|---|---|
| PD-R01 | 1280 桌面全导航 | 主导航、工作台、表格和全屏详情可见；多数页面利用宽度保守，需在 1920/4K 验证最大宽度与信息密度 |
| PD-R02 | 390×844 商品发布 | 无页面级横向滚动；侧栏折叠，表单单列，内部 `.app-main` 可滚动；买家预览仍在 DOM，需要显式的“表单/预览”切换而非仅下沉 |
| PD-R03 | 390×844 消息管理 | 三页签和会话列表可见，三栏按纵向堆叠；需补列表→会话→资料的明确层级和返回，不应在一页无限向下堆叠 |
| PD-R04 | 640×360（200% 等效）商品 360 | 无页面级横向滚动，但页签区域被固定头尾挤到视觉不可见；底部 9 个动作占据大半屏，P1 阻断 |
| PD-R05 | 键盘/语义 | 商品/订单/买家详情与客服页签有 `tablist/tab`，主要对话框有 `dialog`；部分普通按钮页签缺 `role=tab` 或切换无效 |
| PD-R06 | 滚动 | 手机发布页唯一主滚动容器可滚到下方；当前全局仍需覆盖用户报告的滚轮被抢回顶部问题、路由返回位置和刷新不抢滚动 |

## 4. 本轮确认的 P1 基线缺陷

### 已由独立测试回传

1. `W3-BUG-001`：买家 360 把未采集订单/商品金额显示为 `¥0.00`。
2. `W3-BUG-002`：数字字符串时间戳在买家会话中直接显示 13 位机器码。
3. `W3-BUG-003`：200% 等效视口商品 360 页签视觉丢失。
4. `W3-BUG-004`：买家 360 缺黑名单状态、管理入口和与客服工作台互链。
5. `W3-BUG-005`：买家 360 的新增/移除黑名单和人工暂停缺少统一操作审计，无法按请求 ID 还原字段前后值。

### 本轮点击新发现

1. `V6-CLICK-001`：连接列表账号卡点击不进入详情，`/connection/:id` 为空白。
2. `V6-CLICK-002`：商品矩阵首次加载会短暂或长时间把范围显示为 0/未同步，稳定后实际为 343 件；需消除错误空态闪烁与请求竞态。
3. `V6-CLICK-003`：商品任务默认空列表与商品页“最近任务”并存，范围或数据源解释不一致。
4. `V6-CLICK-004`：运营中心顶部四页签点击后仍停留“使用向导”。
5. `V6-CLICK-005`：订单列表跨店/单店加载不结束或返回 0，无法从列表进入已有 QA 订单详情。
6. `V6-CLICK-006`：客服从买家会话切到通知/AI 后账号范围丢失并显示“暂无可用账号”。
7. `V6-CLICK-007`：买家列表和深链被长期加载阻塞。
8. `V6-CLICK-008`：通知与诊断页签点击不切换，正文为空。
9. `V6-CLICK-009`：系统设置侧栏点击后正文不切换或长期加载。
10. `V6-CLICK-010`：团队与权限持续读取，成员数据不可见。
11. `V6-CLICK-011`：扫码二维码缺账号 ID/备注、生成时间、失效时间、倒计时和刷新。
12. `V6-CLICK-012`：手机客服仍把列表、会话和资料纵向堆在单页，操作路径过长。

## 5. 不得误判为已实现的能力

- 有搜索框不等于已验证全站搜索；商机、比价必须返回来源、时间、样本、分页和错误证据。
- 有“个人号/官方/鱼小铺”选项不等于三通道可发布；只有能力探测和平台回执才可标为可执行。
- 有商品属性字段不等于平台动态类目已接入；当前目录是本地参考目录。
- 有批量任务弹窗不等于 1000 件、重启恢复、未知、权限撤销已通过。
- 有订单退款页签不等于已能在平台同意/拒绝退款；无适配器时必须保持只读/人工路径。
- 有 AI 开关不等于回答安全；必须保存命中版本、证据片段、置信度、策略和送达结果。

## 6. 下一轮取证补充项

在修复 `V6-CLICK-001～010` 后，必须重新点击并补齐：连接详情所有操作、运营中心四页签及资源弹窗、订单列表→五页签、买家列表→四页签、诊断五页签、设置七分区、成员创建/编辑预检，以及空态、错误、无权限、部分成功、结果未知和 1000 件状态。修复前的空白或加载截图保留为缺陷证据，不用新截图覆盖历史。

## 7. v3.0.2 第一批运行态复验

本批按相同 URL 和真实 QA 数据重新点击，旧缺陷截图继续保留：

| 证据 | 复验范围 | 结果 |
|---|---|---|
| PD-036 | `/connection` 账号 101→102 | 账号卡具备按钮/选中语义；右侧详情与 `?accountId=102`、完整档案 `/connection/102` 一致，关闭 `V6-CLICK-001` 的入口断裂部分 |
| PD-037 | `/operations?view=resources&type=MATERIAL` | 四个主栏目正文、URL、当前资源类型同步；前进后退可恢复；主栏目具备 tablist/tab 语义；18 条真实素材稳定展示 |
| PD-038 | `/operations-health?tab=exceptions` | 五页签可切换并保持 URL；真实空态为“暂无异常待办”，不再显示空白正文 |
| PD-039 | `/settings?panel=ai` | 七分区侧栏切换真实正文与 URL；AI 不可用状态、原因和当前模型事实可见 |
| PD-R07 | 390×844 通知渠道 | 顶部页签可横向访问，渠道卡片、状态和操作按钮完整，无页面级横向滚动 |
| PD-R08 | 390×844 系统设置 | 七分区改为单行横向栏目，正文获得主要视口高度；当前栏目与深链一致 |
| PD-R09 | 390×844 运营中心 | 主栏目、资源类型和表格分别使用明确横向边界；主操作可达，表格不撑宽页面 |

设计复验控制台无新增 warning/error。该批只关闭上述入口与页签断裂，不代表订单、客服、买家加载、团队权限、二维码和手机客服等其余点击缺陷已经完成。

## 8. v3.0.3 第二批运行态复验

本批继续沿用 Tenant-A、OWNER、账号 101/102/103 和 `QA-*` 只读数据。复验期间不发送买家消息、不提交二维码扫码、不执行发布、发货、退款或其他平台写入。

| 证据 | 复验范围 | 结果 |
|---|---|---|
| PD-040 | `/orders?accountId=102&orderId=QA-ORDER-0091` | 300 条隔离订单数据下可按账号、买家或订单号检索；订单号深链稳定收敛为 1 条并自动打开全屏详情，账号、金额、履约、退款和来源与列表一致；关闭 `V6-CLICK-005` 运行态入口阻塞 |
| PD-041 | `/messages?accountId=102` 及 `inbox=notifications/handoffs` | Long 字符串 ID 在 API 边界统一归一化，账号 102 不再因严格类型比较回退到首店；三页签切换和刷新均保持账号与 URL；关闭 `V6-CLICK-006` |
| PD-042 | `/buyers?accountId=102&buyerAccountId=102&buyerId=QA-BUYER-4` | 买家列表稳定展示 4 位买家，深链直接打开 QA Buyer -4 的订单/会话/商品/评价全屏档案；首载、真空态和失败态分开，失败可重试且刷新失败保留上次成功结果；关闭 `V6-CLICK-007` 运行态阻塞 |
| PD-043 | `/connection/101` 扫码更新 | 续期二维码同屏显示闲鱼账号 ID `qa-tenant-a-full`、系统店铺 ID 101、账号备注、生成时间、本地失效时间、剩余倒计时和刷新动作；明确提示平台可能提前失效 |
| PD-044 | `/accounts` 扫码接入 | 明确区分“新增闲鱼账号”和已有账号续期入口；新账号二维码显示生成/失效时间、倒计时、会话 ID和刷新动作，不虚构尚未识别的账号 ID；关闭 `V6-CLICK-011` 的页面信息缺口 |
| PD-045 | 客服→买家/订单跨页 | “查看买家档案”携带 `accountId + buyerAccountId + buyerId`；“查看关联订单”携带 `accountId + buyerId`；目标页按相同店铺和对象恢复，不再丢失上下文 |
| PD-R10 | 390×844 `/messages?accountId=102` | 手机端改为会话列表→当前会话→会话资料三阶段；每阶段只显示一个主工作区，提供“返回会话/返回列表”和“会话资料”动作，无三栏无限纵向堆叠；关闭 `V6-CLICK-012` |

本批顺带修正订单“旗帜”字段的 `NONE` 文案：协作标记显示“无标记”，退款筛选仍显示“无退款”，两种业务语义不再复用同一标签。

截至本批开发复验，`V6-CLICK-001/004/005/006/007/008/009/011/012` 已有运行态关闭证据；`V6-CLICK-002/003/010` 仍是 Gate 3.1 未决项，且全部关闭结论仍需独立测试回归确认。

## 9. v3.0.4 客服账号隔离缺陷复验

独立测试对 `v3.0.3-rc.1` 回归后，确认 `V6-CLICK-002/003/005/007/010/011/012` 通过，但回传 P1 `G31-BUG-001`：从账号 102 买家档案进入客服、切换 AI 待接管后刷新，URL 仍为 102，页面却会先读账号 101 并展示其他买家。

| 证据 | 复验范围 | 结果 |
|---|---|---|
| PD-046 | `/messages?accountId=102&buyerId=QA-BUYER-4&inbox=handoffs` 刷新 | 稳定选中 `QA Partial Coverage Shop` 和“AI 待接管”；URL 保留 102/买家/页签；不出现账号 101 或 `QA Buyer -9` |
| PD-047 | 客服通知↔AI 待接管前进后退 | 账号 102、URL、页签选中态和正文一致，浏览器历史可恢复 |
| PD-048 | 快速切换 101→102 | 请求修订号废弃过期消息、商品、会话范围和待接管响应；最终只显示 102，无短暂串店 |
| PD-049 | `/messages?accountId=201&inbox=handoffs` 未授权账号 | 保留原 URL，只显示“无权访问该店铺”及返回入口；不默认切到首店，不展示任何店铺业务数据 |
| PD-R11 | 390×844 待接管与无权限态 | 账号/页签与桌面一致；无权限态不渲染业务面板，页面无横向溢出 |

页面初始化期间只显示“正在载入客服账号范围”骨架，路由账号授权确认前不发起店铺消息、商品、会话与待接管数据展示。部署后的浏览器检查没有新增 warning/error；历史 Network Error 发生在本机应用重启窗口，不计入稳定态结果。

`G31-BUG-001` 已具备开发侧关闭证据，仍需独立测试在冻结提交上回归；未收到通过结论前，Gate 3.1 保持未完成。
