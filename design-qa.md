# Product Design QA — v4.0.0-rc.1 系统治理与相邻回归

## Wave 13 — PRD-06 商品数据与事件证据

- implementation URL: `http://127.0.0.1:3000/goods?detailAccountId=101&goodsId=QA-GOODS-0999&detailTab=metrics`。
- reference: 用户提供的鱼麦多店铺详情截图；只参考全屏档案、稳定上下文、证据与下一步同屏的设计原则，不复制品牌、文案或素材。
- desktop: 1440×900 下数据窗口按 1/7/30 天固定排序；窗口级来源、覆盖、日期、同步时间与字段定义可见。事件页默认中文显示业务影响和下一步，机器码收进高级证据。
- mobile: 390×844 下数据卡和事件时间线单列；首轮发现底部固定动作形成四行并遮挡正文，修复为单行横向动作轨后，正文、页签、关闭和首要操作均可达。
- 200%/4K: 640×360 等效窄屏下关闭、页签、正文最小高度和横向动作轨可达；3840×2160 保持受控最大宽度，没有裁切或失控拉伸。
- truth: `QA-GOODS-0999` 无指标样本时展示 `—`、检查范围、最后检查、原因和下一步，明确“不代表 0”；1/7/30 天不会闪成 0。多个来源只能是部分覆盖。
- consistency: 最终部署只读复核发现列表顶部曾把 7 天范围的 1 天样本显示为“完整”；修正后稳定显示“1/7 天 · 部分”，与 `QA-GOODS-0000` 详情的字段级证据一致。
- states: 观察到加载骨架、未同步空态、36 条事件、大数据 1013 件/51 页、结果未知安全动作；无权限由既有账号数据范围与 403 测试覆盖，本轮没有修改权限。
- interaction: 真实滚轮下滑后页面没有自动返回顶部；Escape 关闭全屏档案并恢复 `/goods` 列表上下文；最终控制台 warning/error 为 0。
- evidence limitation: 浏览器工具只提供本轮会话内联截图，没有可提交的本地图片路径。

result: passed（桌面、390×844、640×360、4K、加载、空态、未知、大数据、滚轮和键盘回归通过）

---

## Wave 8 最终复核

- implementation URL: `http://127.0.0.1:3000`
- implementation screenshots: Codex In-app Browser tab 9 的本轮内联截图；工具未提供可提交的本地截图路径。
- viewports: 默认桌面、390×844、640×360（高缩放等效窄屏）、3840×2160；检查结束后已恢复默认视口。
- routes: `/settings?panel=menu`、`/settings?panel=ai`、`/admin/users`、`/operations-health`、`/workflows`、`/operations?view=resources&type=MATERIAL`。
- safety: 菜单顺序只在草稿中交换并恢复，未保存；成员权限弹窗只读后取消；未触发密码重置、两步验证绑定、外部通知或平台写操作。

### Findings

本轮没有未解决的 P0/P1；4K 的超宽信息密度后续可作为 P3 继续优化，但不存在裁切或功能不可达。

- 系统设置：菜单管理在桌面与手机均显示“当前已保存 / 保存后”，修改草稿立即显示“待保存”，恢复后回到“无变更”；明确说明菜单排序不会增加权限。
- 密钥设置：API Key 输入框始终为空，页面明确“仅写入，保存后不会再次显示”；没有把服务未启用显示为可用。
- 两步验证：未启用时只显示“绑定认证器”，没有在登录外无条件展示验证码输入；状态区可承载恢复码数量。
- 团队权限：成员列表、角色、状态、账号范围、动态分组、权限项和保存前范围摘要可见；手机为单列卡片；弹窗正文独立滚动、底部动作固定。
- 团队权限弹窗回归：账号创建/编辑和密码重置均具备模态语义、可读标题、初始焦点、Tab 约束、Esc 关闭和焦点恢复；检查过程未保存任何权限或密码变更。
- 诊断页：未知值与真实 0 分开；自动更新代理未就绪时，版本卡与顶部未知计数同步降级，不再显示绿色“当前最新”；系统提醒、业务待办和外部投递失败分组展示；每项具备影响、处理建议、数据来源和证据时间。
- Wave 7 P1 回归：工作流运行与素材 360 档案均具备 dialog 语义、自动聚焦、Esc 关闭与焦点恢复；素材顶部当前版本 `v1` 与不可变版本 `v1 ACTIVE` 一致。
- 响应式：390×844 无页面级横向溢出；640×360 首要标题、刷新和状态页签可达；3840×2160 导航、统计和诊断卡不裁切。
- 控制台：最终浏览器 warning/error 为 0。

### State coverage

| 状态 | 证据与结论 |
|---|---|
| 加载 | 诊断页先显示“正在读取”，事实返回后再展示计数，没有用 0 代替加载态。 |
| 空/未配置 | 密钥字段为空并用 `configured` 语义表达；两步验证未启用有明确下一步。 |
| 错误/未知 | 更新或外部服务不能验证时进入未知/提醒，不显示绿色正常。 |
| 无权限 | 菜单排序明确不改变服务端授权；成员操作继续依赖后端角色、动作和账号范围。 |
| 大数据 | 团队成员列表和 304 条素材列表可滚动/分页；360 档案按需读取。 |
| 键盘 | 两个 P1 弹窗初始焦点、Esc 关闭和焦点返回实测通过。 |

final result: passed（P0/P1 = 0）

### Wave 9 — ACC-11 账号运行档案隔离证据

- scope: 账号矩阵 → 店铺 360 → 账号运行档案卡。
- desktop: `BPR-xxxxxxxxxxxx` 档案 ID 使用整行等宽展示；隔离状态、账号级存储范围、平台、视口、时区和浏览器状态层级清楚。
- mobile: 390×844 下详情全屏单列，dialog/document 宽度均为 390px，无页面级横向溢出；固定底部操作仍可用。
- states: 正常状态显示“隔离正常”；冲突显示红色“发现冲突”与停止相关账号/重建档案建议；检测异常显示“隔离待检测”，不会误报正常。
- privacy: 页面只显示稳定短 ID 和不可逆结论，不返回 Cookie、Token、storage state 或其 SHA-256。
- iteration: 首轮截图发现短 ID 被两栏拆行、结论与隐私说明缺少分隔；修正为整行 ID 和完整句号后复核通过。
- evidence limit: 浏览器工具提供本轮内联桌面/390px截图但不提供持久化路径；未为视觉截图人为制造数据库冲突。

result: passed（ACC-11 增量设计 QA）

---

## V6 Wave 10：全店立即擦亮增量 QA

- flow: 店铺 360 的“全店擦亮” → 当前账号全部在售商品 → 自动擦亮预检；未创建任务或触达平台。
- desktop: 首轮 1280×720 截图发现长排除列表把底部动作挤出首屏；修复为固定页头/页脚、正文单滚动后，97 件范围、限速、预检摘要、确认输入和底部动作均可达。
- mobile: 390×844 下弹窗单列、无页面级横向溢出；滚动到底后确认输入完整可见，取消/创建按钮常驻。
- trust: 页面明确“这里只做预检，不会直接执行”，并展示账号 101、97 件、97 可执行、0 冲突及“隔离 QA Mock，不触达平台”。
- accessibility: 专用标题与 `aria-labelledby` 关联；锁定选择器仍有标签；完整确认前创建按钮禁用。
- evidence: 本轮运行态内联截图；浏览器工具未提供持久化截图路径。

result: passed（首轮发现并修正固定底部缺陷）

---

## V6 Wave 11：多账号批量动作增量 QA

- flow: 账号矩阵跨页选择 → 范围预检 → 启用/停用/连接运行态同步/续期 → 逐账号持久任务；本轮只读预检，没有创建任务。
- desktop: 1280×720 下居中弹窗，动作与范围并列，选中/可执行/冲突/通道四项同屏，候选原因、风险确认和固定底部动作可达。
- mobile: 390×844 下全屏单列，页头/页脚固定、正文单滚动；首轮发现候选列表被内容挤压成滚动条，补 `min-height` 后账号名、ID、连接、授权和冲突原因完整可读。
- trust: SYNC 明确只覆盖消息连接运行实况；画像和处罚仍以各自快照为准。正式停用影响范围、续期需对应账号扫码、QA Mock 不触达平台均在执行前可见。
- accessibility: 原生 checkbox/select/button；逐账号选择有可读名称；未完成风险确认或无可执行项时创建按钮禁用；关闭按钮可见。
- safety: 只选择账号 101 并执行 preview；没有勾选确认、没有创建任务、没有修改真实账号 202/203。

result: passed（桌面、手机、冲突态和长内容滚动通过；独立任务继续验收状态机）

### 独立测试 P1 修复复核

- error recovery: 预检 400/403/409/500/超时统一进入持久错误区，保留选择上下文并阻止创建；错误区提供返回调整和重试，避免空白弹窗与只靠瞬时提示。
- accessibility: 弹窗打开后焦点进入首个可操作项，Tab/Shift+Tab 保持在弹窗内，Escape 可关闭，关闭后回到原触发按钮；背景内容在弹窗期间隔离。
- idempotency trust: 服务端按规范化完整载荷指纹判定同请求回放，账号顺序不影响等价性，账号集合、排除项或筛选变化均明确拒绝。
- desktop/mobile evidence: 1280×720 与 390×844 均以账号 101 + 202 的只读混合预检复核；精确错误、2 个账号范围、返回调整、重试和禁用创建同时可见。390 宽下动作纵向排列，固定底部不遮挡错误内容，无页面横向溢出。
- keyboard evidence: 初始焦点落在动作选择器；连续 Shift+Tab 从首部绕回底部取消按钮；Escape 关闭后焦点恢复到“批量操作”触发按钮。

result: passed（V6-W11-QA-001/003 设计回归通过；未创建任务、未改动真实账号）

---

## V6 Wave 12：商品营销增量 QA

- flow: 商品 360 → 营销证据 → 配置粉丝价/小刀/闲鱼币 → 预检 → 本地草稿或隔离 QA 应用。
- desktop: 全屏商品档案内使用双列营销卡；配置弹窗居中，平台证据、预计成本、范围与固定动作区清晰。首次预检发现旧 JAR 前端仍对数字执行 `trim()`，重新部署并修正磁盘资源优先级后通过。
- mobile: 390×844 下营销卡单列、配置弹窗全屏；页头和两行底部动作固定，正文可滚动到闲鱼币、SKU 覆盖、警告和确认输入，无横向裁切。
- truth: 方案值与平台观察值并列；方案来源与平台来源独立。临时草稿实测显示本地 17.90 / 平台 18.00，恢复后两侧均 18.00；未知余额为 `—`。
- scope: 主档声明 4 个 SKU、明细验证 0 个时显示 `0 / 4` 和部分覆盖警告，不再写成“影响 0 个 SKU”。清空两个粉丝价档位的预检明确列出“老粉价、已购粉价”和删除数量 2。
- degradation: 推广、曝光归因和佣金显示安全降级且按钮禁用；QA Mock 只开放粉丝价、小刀和闲鱼币的本地状态机，不把推广标成已验证。
- accessibility: 初始焦点进入首个粉丝价；长内容滚动可达；嵌套弹窗只允许最上层处理 Esc/Tab，单次 Esc 后外层商品档案保留，焦点恢复到“配置营销方案”。
- safety: 只使用租户 1、账号 101、商品 `QA-GOODS-0999`；删除档位只做 preview；QA apply 明确 `platformNetworkCalls=false`，真实账号 202/203 未执行营销写入。

result: passed（桌面、390×844、预检、部分证据、长内容、错误修复和键盘回归通过）

---

## 历史：v3.4.0-rc.1 素材、增长与工作流

## 对照证据

- source visual truth: `/tmp/codex-remote-attachments/01a06ce4-25a6-7660-90e5-fd32bbf02361/A649EEC7-2EF6-4971-80BC-8C8159DABA23/1-照片-1.jpg`
- source pixels: 1280×741。用于对照鱼麦多成熟的信息架构、暖黄主操作、白色工作台、风险可执行、详情不丢上下文和固定操作区；没有复制其品牌、文案、头像或素材。
- implementation URL: `http://127.0.0.1:3000`
- implementation screenshots: Codex In-app Browser tab 6 的本轮内联截图；浏览器工具未暴露可持久化截图文件路径。
- viewports: 1920×1080、3840×2160、390×844；最终恢复为 1920×1080。
- routes: `/operations`、`/opportunities`、`/price-comparison`、`/supplies`、`/workflows`，并相邻复核 `/accounts?accountId=101` 的全屏店铺 360。
- states: 正常、真空态、保留旧值的加载/刷新、持久错误与重试、无权限实现、大数据量、失败、结果未知、已取消、可补偿和任务详情。

## Findings

最终复核没有未解决的 P0/P1/P2。

- 信息架构：运营中心承载素材版本与商品映射；商机、比价、货源和工作流各自只有一个真实入口。工作流详情在同页展示定义、版本、节点证据和任务中心，不丢账号及选中上下文。
- 布局与滚动：桌面工作流为左侧定义、中部画布、右侧节点配置；画布可横向滚动且不抢页面纵向滚轮。390 宽下改为单列节点列表；编辑器标题/底部固定、内容单滚动；真实滚轮滚动后未自动回顶。
- 视觉语言：统一暖黄主操作、低饱和背景、白色卡片、细边框和克制阴影；危险/未知/成功同时使用文字和结构，不只靠颜色。
- 未知值保真：素材未设置价格显示“价格未设置”，API 为 `null`；未同步、不可用和真实 0 明确区分；没有把未知金额、样本和库存渲染成 0。
- 来源与风险：商机、比价和素材把来源、采样时间、许可、样本与口径置于可见区域；工作流顶部明确“平台写入：关闭”，发布节点标注“仅结构预检”。
- 大数据：素材 143 条、货源 161 条、工作流运行 1000 条均分页；翻页后范围文案和首项同步变化，未一次性渲染全部记录。
- 响应式：390×844 没有页面级横向溢出，按钮和账号选择可读；1920×1080 主操作首屏可达；3840×2160 内容随可用宽度扩展、画布不裁切。
- 账号 360 相邻回归：保持全屏详情而非抽屉；固定页头/页脚，左侧画像与经营卡、右侧风险摘要与事件，符合参考图“风险可执行、详情不丢上下文”的原则。
- 可访问性：工作流名称、执行账号、版本说明、运行状态筛选、商机/比价账号选择和货源搜索均有可读名称；主要动作使用 button/select/input 语义；最终浏览器控制台 error/warn 为 0。

## Comparison history

1. 第一轮发现：素材未填写价格被旧表结构的 0 显示为 `¥0.00`，会把未知冒充成事实；资源和任务大列表缺少可操作分页。
   - 修复：API 从版本载荷恢复“是否显式填写”的语义；未设置返回 `null`；素材 25 条/页、任务 25 条/页、货源 24 条/页。
2. 第二轮发现：手机资源编辑器整页滚动，标题和底部动作会离开视口；商机账号选择文字被窄屏截断。
   - 修复：编辑器改为固定标题/底部和单独滚动正文；手机账号/模式选择改为整行宽度；补全部分缺失的 aria-label。
3. 最终复核：
   - 1920×1080：三栏工作流、画布、节点配置、版本和任务中心稳定。
   - 390×844：统计、表单、定义列表、节点列表和任务详情单列可达；首载统计在事实返回前显示“—”并标记忙碌，稳定后显示真实 6/1/1/0。
   - 3840×2160：最大视口兼容；画布宽度和分页区域可用，无裁切。
   - 最终部署后控制台 warning/error 均为 0。

## State coverage

| 状态 | 证据与结论 |
|---|---|
| 加载/刷新 | 组件区分首载与刷新；首载统计显示“—/忙碌”，刷新保留上次成功内容，事实返回后显示真实 6/1/1/0。 |
| 空状态 | 没有定义、素材、搜索结果或任务时显示原因与下一步，不保留空画布冒充可用工作流。 |
| 错误 | 请求错误保留最后成功值，显示错误原因和重试入口；服务重部署窗口不固化为业务空态。 |
| 无权限 | 后端按菜单、动作、租户和账号范围拒绝；页面只显示不可执行/无权限状态，不回退到另一店铺数据。权限测试覆盖 403 和数据范围注入。 |
| 大数据 | 143 素材、161 货源、1000 运行分页实测；当前页 DOM 数量受控。 |
| 结果未知 | 节点进入 UNKNOWN 后禁止自动重试，任务详情提供人工核对动作和证据。 |
| 取消/补偿 | 只取消未开始节点；只对本地有副作用的 COLLECT/MATERIAL 执行补偿，其余显示 NOT_REQUIRED。 |

## Open Questions / P3

- 当前 Product Design 工具只返回会话内联截图，没有提供持久化文件路径；报告记录了 tab、URL、视口和版本，但尚不能生成项目内 PNG 证据包。
- 200% 缩放等效窄屏由 390×844 与既有 640×360 规则覆盖；Wave 8 最终总门禁仍需用独立测试环境补一次真实浏览器缩放/读屏回归。
- 商机真实平台采集和分销佣金需要单独的平台授权与归因协议；当前安全降级为不抓取/隐藏入口，不构成本批缺陷。

## Implementation Checklist

- [x] 素材版本、来源、许可、去重、账号范围与引用影响
- [x] 商机/比价来源、采样时间、样本、异常值和口径
- [x] 货源库存、时效、退换、有效期、商品映射与分页
- [x] 工作流版本、预检、实例、逐节点证据、重试、取消、未知和补偿
- [x] 桌面 1920、4K、手机 390、滚动和文本溢出
- [x] 加载、空、错误、无权限、大数据、未知和取消状态
- [x] 最终浏览器控制台 warning/error 为 0

final result: passed

---

## V6 Wave 14：商品发布内容风险预检增量 QA

- flow: 商品发布工作台 → 本地结构校验 → 内容策略风险卡 → 返回问题字段；风险存在时没有进入平台预检或生成预检凭证。
- reference: 对照用户提供的鱼麦多店铺详情与既有商品发布参考，只采用风险可执行、来源可见、详情不丢上下文和暖黄主操作原则，没有复制品牌、文案或素材。
- desktop 1080P: 1920×1080 下结构缺项与内容风险分成两张卡，命中字段、风险词、片段、建议、策略版本、来源和时间均在发布区可见；右侧买家预览保留当前草稿上下文。测得 `scrollWidth=clientWidth=1920`。
- 4K: 3840×2160 下发布导航、六段表单、买家预览和风险结果保持受控三栏关系，没有拉伸裁切或页面级横向溢出。
- mobile: 390×844 下风险卡改为单列；长建议完整换行，底部保存/校验/确认动作可达；真实滚动多页后没有自动回顶。
- navigation: 点击“商品详情 / 站外交易”风险项准确返回商品详情输入框，保留原值和风险上下文。
- truth: 本地词库明确标为“系统内容策略”，策略版本为 `terms-*`；页面没有将其宣称为闲鱼实时规则。阻断时确认发布禁用，API 无 `previewToken`。
- states: 空表单保留结构化必填错误；风险表单显示阻断和修复建议；安全内容由服务单测覆盖明确通过态；平台适配能力未验证时继续安全降级。
- console: 最终浏览器 warning/error 为 0。
- evidence limitation: 浏览器工具仅提供本轮会话内联截图，没有持久化 PNG 路径；本记录保存 URL、视口、测量和交互结论。

result: passed（桌面 1920×1080、4K、390×844、风险定位和滚动复核通过）

---

## V6 Wave 15：商品 1/7/30 天买家漏斗增量 QA

- flow: 商品管理 → 商品 360 → 数据罗盘 → 1/7/30 天买家漏斗；完整态使用 `QA-GOODS-0000`，空态使用 `QA-GOODS-0999`。
- reference: 采用鱼麦多评审证据中“1/7/30 天、PV/UV 分离、来源与时间可见”的成熟原则；完成和退款从支付买家分支展示，没有复制品牌、文案或素材。
- desktop 1080P: 1920×1080 下曝光、详情、咨询、支付四级核心链路与完成/退款结果分支同屏；辅助 PV/订单/金额和来源证据层级清楚。测得 `document.scrollWidth=document.clientWidth=1920`，档案无内部横向溢出。
- 4K: 3840×2160 下档案居中且宽度受控，漏斗节点不被无限拉长；固定页头、页签、滚动正文和底部动作保持完整。
- mobile: 390×844 下核心漏斗变为纵向阅读，转化率和覆盖状态置于节点之间，完成/退款分支改为单列；档案与 document 均为 390px，无页面级横向溢出。
- scroll: 手机档案正文 `scrollHeight=4512/clientHeight=599`；滚轮从 `scrollTop=0` 到 `844` 后保持稳定，没有自动回到顶部。
- truth: `QA-GOODS-0000` 的完整 1 天样本明确显示真实 0（完成买家 0）和真实 100%（退款/支付）；7/30 天部分覆盖不冒充完整。`QA-GOODS-0999` 未同步时全部显示“—”，并说明范围、检查时间和下一步。
- formula: 页面把曝光次数、咨询次数、订单数和金额移到辅助区；核心漏斗只使用独立买家人数。覆盖不兼容时显示“证据不足”，分母为真实 0 时显示“不可计算”。
- source: `QA_FIXTURE` 最终显示为“隔离测试数据”；平台适配器未接入保持显式安全降级。
- console: 最终浏览器 warning/error 为 0。
- evidence limitation: Product Design 浏览器只提供会话内联截图，没有持久化 PNG 路径；本记录保存 URL、视口、DOM 尺寸和滚动测量结论。

result: passed（1920×1080、3840×2160、390×844、完整/部分/空态和滚动复核通过）

---

## V6 Wave 16：商品逐日趋势与经营报表增量 QA

- flow: 商品管理 → 导出当前筛选；商品 360 → 数据罗盘 → 30 天逐日趋势与 1/7/30 天指标窗口。
- reference: 采用鱼麦多评审证据中“趋势、筛选范围、来源与时间可见”的原则；没有复制品牌、文案或素材。
- desktop 1080P: 1920×1080 下 30 天趋势、指标选择、覆盖、来源、同步时间和固定底部动作层级清楚；鼠标滚轮连续下滚两页后保持在最近 30 天窗口，没有自动回顶。
- 4K: 3840×2160 下全屏档案宽度受控，30 根日趋势柱没有被无限拉伸或裁切，页头、页签、正文和底部动作均可见。
- mobile: 390×844 下页签、趋势图和底部动作采用各自局部横向滚动；趋势卡、指标选择和来源说明单列可读，页面主体不被图表撑宽。
- truth: `QA-GOODS-0000` 显示 2/30 天已同步，缺失 28 天均为“—”；`QA-GOODS-0999` 使用完整解释型空态，明确未同步不代表 0。
- conflict: 临时双来源 API 证据确认趋势当天为 `CONFLICT/MULTIPLE` 且值为空，1 天窗口为 `SOURCE_CONFLICT` 且汇总、漏斗和金额全部隐藏；临时行随后已删除。
- loading: 切换商品时显示“正在读取商品档案…”，旧商品内容没有冒充新商品事实。
- error/no access: 访问不属于当前经营主体的账号/商品时关闭档案，保留空筛选结果，并用警示条显示“商品不存在或不属于当前经营主体”。
- large data: 347 件列表继续分页，表格首屏数量受控；导出明确是当前筛选范围而非当前页。
- accessibility: 趋势区有可读 region/img 名称，每个点提供日期、指标值与来源；观察指标使用原生选择框，空态为 status，错误为 alert。
- evidence limitation: Product Design 浏览器只提供本轮会话内联截图，没有持久化 PNG 路径；本记录保存 URL、视口、状态与交互结论。

result: passed（1920×1080、3840×2160、390×844、滚动、加载、空态、错误/无权限和大数据状态通过）
