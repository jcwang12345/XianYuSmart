# Product Design QA — Wave 5 履约库存、固定模板与订单全景（v3.2.0-rc.1）

- QA 日期：2026-09-15（Asia/Shanghai）
- QA 环境：macOS 裸机 Java/Vue `http://127.0.0.1:3000`；Docker 仅复用 MySQL 5.7 `127.0.0.1:13306`，Flyway schema 53。
- 来源视觉真值：`/tmp/codex-remote-attachments/01a06ce4-25a6-7660-90e5-fd32bbf02361/A649EEC7-2EF6-4971-80BC-8C8159DABA23/1-照片-1.jpg`（1276×735）。参考其宽画布店铺详情、左右信息架构、风险列表和常驻操作区；未复制品牌、文案、图标或素材。
- 实现证据：本轮由 Codex in-app Chromium 直接采集并内嵌留证，浏览器接口未返回可写本地截图路径。全视图与聚焦截图覆盖店铺 360、订单全屏详情、卡密仓库、供货请求、人工核对弹窗和固定模板弹窗。
- 视口与密度：1920×1080、390×844、3840×2160；按 1 CSS px 对 1 截图 px 记录。4K 下店铺详情为居中 1440×900，页面宽度无溢出。
- 安全边界：只对 Tenant 1、QA 账号 101/102/103 和 `QA-*` 夹具执行本地写入；真实账号 202/203 仅展示和读取。没有发布、改价、上下架、删除、发货、评价、退款、消息发送或外部供货调用。

## 全视图与参考比较

- 店铺 360 已从抽屉改为独立居中详情层：首屏保留店铺身份、会话/授权、数据看板和刷新；左侧等级、指标、信用与接入证据，右侧处罚摘要、筛选和风险卡片，整体结构与参考图一致但使用本系统设计语言。
- 订单 360 使用完整视口，不再挤在侧边抽屉；页签、对象身份和返回路径常驻，概览将平台事实、本地协作、能力边界和评价能力分开，未验证能力不提供假入口。
- 卡密仓库形成“仓库—状态摘要—明细/事件/供货请求”的稳定层级；结果未知可进入人工核对，但必须先生成确认范围，确认前最终按钮禁用。页面明确不会再次取卡或自动发给买家。
- 固定内容模板把多账号范围、内容、变量、服务端预览、版本/引用证据放在同一编辑上下文。手机端长表单的标题吸顶、取消/保存吸底，滚动 183.5px 后仍保持可达。

## 状态、响应式与可访问性 QA

- 默认与加载：卡密明细和模板列表首载显示明确加载状态，完成后布局不跳回顶部；全局滚轮回弹回归中，店铺详情滚动容器从 0 到 526px 后保持位置。
- 空状态：账号 101 尚无固定模板时显示“暂无固定内容模板”并给出创建入口；未知数据保持 `—`，不伪造为 0。
- 错误与结果未知：外部供货超时夹具显示订单、数量、尝试次数、熔断和下一步；密钥、令牌、载荷指纹及卡密正文均不回显。通用请求错误继续沿用既有保留旧数据/重试模式。
- 无权限：沿用已验收的只读角色菜单和写按钮禁用；本批新增库存事件/外部供货读取被归入卡密菜单权限，人工处置、库存写入和模板写入继续要求对应动作权限，自动化回归覆盖 403 门禁。
- 大数据量：300 条订单分页、120 条库存事件夹具、长能力矩阵和多账号模板选择均未破坏桌面或手机布局。
- 窄屏：390×844 下店铺详情与订单详情均占满视口，底部操作常驻；卡密仓库先选择仓库再进入详情，表格转为卡片；人工核对弹窗转底部面板；无页面级横向溢出。
- 4K：3840×2160 下主内容居中且不无限拉伸，详情层保持合理最大宽度，空白区不会挤压信息层级。
- 键盘与语义：关键栏目使用 `dialog`、`tablist/tab`、表格与具名按钮；关闭、返回、预检和最终确认都有可访问名称。
- 控制台：最终部署和上述页面复验后，浏览器 warning/error 为 0。

## 问题与修复历史

1. P2：手机端固定模板长弹窗原本只有滚到最底部才能保存。已增加标题吸顶与操作区吸底，并把两个动作扩大为至少 44px；重新构建、部署和滚动复验通过。
2. P2：订单详情仍沿用旧抽屉类名且视觉上受限。已改为 `100vw × 100dvh` 全屏详情并保留列表滚动与路由上下文；桌面/手机复验通过。
3. P1：外部供货结果未知曾缺人工闭环。现有预检、精确数量、幂等处置、待核对卡密、事件与审计；确认按钮必须显式勾选，且不会自动发送。
4. P1：订单时间线曾看不到评价邀请/跟进重试。已合并持久化互动事件并显示状态、尝试次数、错误和下次重试时间。
5. `XYM-FUL-001 / P2`：独立测试发现固定模板弹窗关闭后焦点落到 `BODY`。现已接入统一焦点陷阱，补齐弹窗语义、背景隔离和首尾循环；Escape、右上角关闭、取消三种路径在 1920×1080 与 390×844 下均恢复到原“编辑”按钮。
6. 同轮视觉复核发现模板卡片操作文字在窄卡片中被挤为竖排。卡片最小列宽已调整，操作保持横排；390×844 下操作区移到标题下方，截图复核无横向溢出。
7. 复测结论：本批没有未决 P0/P1/P2 设计缺陷。

## 构建和运行证据

- 前端类型检查：通过。
- Vite 生产构建：357 modules transformed，通过；最终产物重新写入 `src/main/resources/static`，未采用测试任务的中间产物。
- Maven 全量测试：247 tests，0 failures，0 errors，0 skipped。
- MySQL 5.7 / Flyway：53 个迁移全部成功，当前 schema version 53。
- QA E2E：`safeFixture=true`、`platformNetworkCalls=false`；4 条库存状态、120 条事件、1 条结果未知请求；只读列表返回 `secretFieldsReturned=false`，人工处置预检返回 `platformWrite=NOT_PERFORMED`。

final result: passed

---

# Product Design QA — 订单售后与退货物流证据（v2.4.1）

- QA 日期：2026-09-14（Asia/Shanghai）
- QA 环境：`http://127.0.0.1:3000` / `http://localhost:3000`，Spring `qa` profile，MySQL 5.7.18
- 范围：ORD-04 退款/售后事实、ORD-05 退货物流证据、订单详情抽屉和“记录平台已确认运单”弹窗
- 安全边界：仅使用 Tenant 1、QA 账号 101/102/103、`QA-ORDER-*`；未调用闲鱼平台写接口，未执行发布、退款、删除或申诉

## 视觉真值与实现证据

- 来源视觉真值：同一 Chrome QA 页面改造前现场截图。1920×968 视口中，订单列表显示“退款处理中 1”，详情却只有“退款 0”空态，缺少售后类型、处理期限、平台下一步和退货物流。
- 最终实现截图：同一 Chrome QA 页面、同一 `QA-ORDER-0000` 数据完成后截图。1920×968 视口中显示退货退款卡、金额事实、退货进度、平台下一步、双期限、买家退回与卖家补发两条物流证据；390×844 视口中抽屉和弹窗均完整可操作。
- 像素说明：浏览器截图像素与 CSS 视口一致；桌面 1920×968，窄屏 390×844。浏览器能力未暴露额外设备缩放倍率，按 1 CSS px 对 1 截图 px 记录。
- 聚焦区域：退款详情卡、物流证据长卡片、二级运单弹窗、安全说明、底部动作区。

## 全视图比较

- 信息架构：售后事实位于订单详情的“退款”页签，不离开订单上下文；物流证据嵌套在对应售后记录下。
- 状态表达：未采集金额显示 `—`，退款状态存在但案例未同步时显示“退款 · 未同步”，没有把未知数据展示成 0。
- 风险表达：弹窗首屏明确说明“不会向闲鱼提交退货、换货或退款决定”；先预检确认范围，再勾选平台事实确认后保存。
- 响应式：桌面双列字段和居中弹窗；390×844 下卡片两列、期限单列、弹窗单列并使用内部滚动与固定底部动作区，无页面级横向溢出。
- 大数据量：300 条订单列表正常分页；售后卡可展示多条物流证据，长请求来源和时间字段未破坏布局。

## 细节检查

- 字体：沿用产品系统字体栈，标题、标签、说明和等宽运单号层级清晰。
- 间距：卡片 12–16px 节奏一致；桌面和窄屏的边距、字段间隔及固定操作区未遮挡正文。
- 色彩：延续闲鱼感的暖黄主色；处理中、运输中和只读提示有文字辅助，不只依赖颜色。
- 图片质量：本功能页无位图内容；图标使用现有矢量组件，无模糊缩放问题。
- 文案：平台事实、本地证据、未同步和安全降级边界均使用中文可执行文案；平台动作不再暴露原始枚举。

## 状态与交互 QA

- 默认/加载：刷新时保留骨架/等待反馈，数据返回后指标、列表和同步说明稳定落位。
- 空状态：`QA-ORDER-0091` 显示“退款状态尚未形成可核验案例”，并提供“重新读取订单事实”；不显示退款 0。
- 错误状态：只读角色请求得到 403 时保留未知指标和空表，不把失败结果当 0；页面同时显示只读权限说明和错误提示。
- 无权限：`matrixqa_support` 仅显示订单、消息、买家菜单；订单页顶部明确“修改、发送和执行操作已停用”，写操作入口不可用。
- 窄屏：390×844 验证订单页、全宽详情抽屉、页签横向滚动、退款卡和底部弹窗。
- 弹窗键盘：Escape 关闭二级弹窗后，焦点返回“记录平台已确认运单”触发按钮；关闭按钮有可访问名称。
- 两步确认：必填项不足时预检按钮禁用；预检返回的确认范围与平台写入“未执行”清晰可见；未确认平台事实前保存按钮禁用。
- 控制台：最终管理员页面未产生本功能新增错误。只读切换前记录到的两条“账号 Cookie 不可用”来自既有买家资料后台请求，不属于本次售后页面实现。

## 问题与修复历史

1. P2：平台下一步曾直接显示 `WAIT_BUYER_RETURN`，窄屏换行差。已改为“等待买家寄回”，保留 `platformActionCode` 供程序判断。
2. P2：卖家补发证据曾覆盖售后主档的买家退回进度。已限制只有 `BUYER_TO_SELLER` 方向更新退款主档状态；最终夹具保持“运输中”。
3. 复测结果：上述两项在桌面和 390×844 最终截图中均未复现。
4. P1 `XYM-ORD-001`：相同 `requestId` 改变载荷曾静默返回旧事实。已增加完整规范化载荷绑定校验；只有完全一致才能重放，任一字段或售后记录变化均返回 409。
5. P1 `XYM-ORD-002`：只读 SUPPORT 曾可调用 QA 售后夹具。已为 QA 控制面增加订单菜单、`action:order-write` 和账号范围三重校验。
6. P1 `XYM-ORD-004`：卖家补发物流事件曾覆盖买家退回主档文案。已与主进度同样按方向隔离，补发事件只显示在对应运单卡片。
7. 复测结果：本批无未决 P0/P1/P2。

## 构建与运行证据

- 无缓存前端类型检查：通过。
- 无缓存 Vite 生产构建：338 modules transformed，通过；最终产物重新写入 `src/main/resources/static`。
- 静态入口校验：`index.html` 引用的 JS/CSS 均存在。
- Maven 全量测试（Docker clean package）：141 tests，0 failures，0 errors，0 skipped。
- Flyway：MySQL 5.7.18 验证 39 个迁移，schema version 39。
- QA E2E：`safeFixture=true`、`platformNetworkCalls=false`、`platformWrite=NOT_PERFORMED`、幂等重放通过。

final result: passed

---

# Product Design QA — Wave 4 商品发布工作台（v3.1.0-rc.1）

- QA 日期：2026-09-15（Asia/Shanghai）
- 来源视觉真值路径：`/tmp/codex-remote-attachments/01a06ce4-25a6-7660-90e5-fd32bbf02361/A649EEC7-2EF6-4971-80BC-8C8159DABA23/1-照片-1.jpg`（1276×735，只作为暖黄配色、宽画布、上下文常驻和风险卡片的信息架构参考，不作发布表单的逐像素模板）
- 实现：本机 `http://127.0.0.1:3000/product-publish`；实现截图由 Codex in-app Browser 在本轮 QA 中直接采集并内嵌留证，浏览器接口未返回可写本地路径。
- 视口与密度：桌面 1920×1080、手机 390×844，均按 1 CSS px 对 1 截图 px；另以 3840×2160 读取布局尺寸确认 4K 居中，主工作台宽 1680px。
- 状态：空草稿、字段错误、已保存 r3、隔离预检、预检后字段变化、发布成功、同请求重放、图片加载错误、手机表单/预览切换。
- 安全边界：Tenant 1、QA 账号 101、`QA-PUBLISH-*`、`QA_LOCAL`；真实账号 202/203 只读，不参与草稿、预检或发布；平台网络调用为 false，平台写入为 `NOT_PERFORMED`。

## 全视图与聚焦比较

- 全视图：桌面采用左侧章节导航、中间单一结构化草稿、右侧买家视角预览；风险、预检和结果证据在原上下文内完成。结构沿用参考图“详情不丢上下文、风险可执行”的原则，没有复制鱼麦多品牌、文案、图标或素材。
- 聚焦区域：店铺/通道证据、三种商品类型、行业→叶子类目→动态属性、图片/视频、售价/原价/库存、规格、履约/售后、预检差异、令牌有效期、发布结果与版本历史均单独检查；这些高密度区域在 1920px 下可读，不需要额外裁切比较。
- 手机：重新载入 390×844 后固定侧栏不存在，主内容宽 379px、无横向溢出；表单与买家预览可显式切换，预览宽 355px，页头、草稿选择和主操作均可达。
- 4K：内容不无限拉伸，1680px 工作台在 3553px 主内容区内居中；没有页面级横向溢出。

## 五项保真检查

1. 字体与层级：沿用系统字体栈；页面标题、章节标题、字段、辅助说明、证据值和机器指纹分层明确，长指纹允许换行。
2. 间距与布局：卡片 12–20px 节奏一致；章节导航和买家预览在桌面常驻，小屏改为单列与显式视图切换；底部发布操作保持可达。
3. 色彩：暖黄只用于主动作和选中态；本地通过、警告、错误和结果证据使用不同语义色，并均有文字标签，不只依赖颜色。
4. 图片与资产：媒体使用真实上传组件，首图标记为封面并支持排序；故意输入不可达 QA 图片时，预览显示“图片无法加载 / 请检查地址或重新上传”，没有用假图掩盖错误。
5. 文案与事实：本地目录明确标记 `LOCAL_REFERENCE`，平台差异明确标记 `DIFFERENT`；未验证的平台能力保持阻断或安全降级，不宣称已经接入。

## 状态、交互与比较迭代

1. 初次预检被 QA 门禁误拒绝：定位到商家运营服务把操作者 ID 当成租户 ID；改为认证租户上下文并补回归测试。修复后 101 预检通过。
2. 初次执行在消费预检凭证时失败：MySQL 5.7 返回 `LocalDateTime`，旧代码强制转换 `Timestamp`；增加双类型兼容与边界测试。修复后生成持久任务 20、商品 `QA-PUBLISHED-20`，状态 `QA_CONFIRMED`。
3. 加载 r3 草稿曾显示“尚未保存”：载入时同步保存状态和更新时间；重新部署后显示“已保存 r3”。
4. 临时从桌面视口直接切到 390px 时响应式监听未收到测试工具的 resize 事件；按真实设备加载方式在 390px 重新载入后侧栏正确移除，主内容全宽。该差异属于测试视口切换方式，不是最终响应式缺陷。
5. 滚轮回弹回归：1920×1080 下主内容滚动从 2749.5px 上移至 1669.5px 后保持位置，没有自动返回顶部。
6. 预检绑定：修改库存 50→51 后“确认发布”立即禁用；自动保存为 r3 并重新预检后恢复。相同请求重复确认仍返回任务 20，没有重复创建。

## 证据边界

- 空态、错误态、桌面、手机、4K、版本历史、预检、发布结果和重复提交已在浏览器实测。
- 全局路由加载提示与只读权限提示沿用已经独立验收的 AppLayout；本轮未为了截图修改用户角色。
- 50 SKU 与大数据任务中心由后端/组件回归和既有隔离夹具覆盖；本轮浏览器主路径使用单规格商品，独立测试仍需复测 50 SKU 的长表交互。
- 本地目录当前是可版本化的参考子集，不是闲鱼官方完整类目库；真实通道对高级属性、SKU、视频和服务协议缺少适配证据时继续阻断真实提交。

独立 50-SKU 与只读角色回归仍是候选发布门禁，但不构成本轮已捕获页面的视觉阻塞。

final result: passed

---

# Product Design QA — 真实账号接入事实一致性（v3.0.5）

- QA 日期：2026-09-15（Asia/Shanghai）
- 环境：macOS 原生应用 `http://127.0.0.1:3000`；Docker 仅复用 MySQL 5.7 `127.0.0.1:13306`。
- 范围：账号矩阵、全屏店铺档案、连接详情、操作日志时间、390×844 响应式。
- 安全边界：两个用户授权的真实账号只做读取和部署后自动重连验证；账号编号与凭据不写入仓库；不执行任何平台业务写动作。

## 设计与事实 QA

1. 账号矩阵：连接摘要与行状态都来自同一次运行时核验；“正常 / 已授权”下补“运行时实测 / 核验时间”，不再显示与连接管理冲突的“待检测”。
2. 店铺档案：接入通道显示来源、核验时间、最近成功和能力边界；画像、处罚未同步仍为 `—`，没有因连接成功被填成 0 或“正常”。
3. 连接详情：两个账号均显示 Cookie 有效、WebSocket 已连接；数字字符串时间显示本地日期，不再出现 `Invalid Date`。
4. 390×844：账号指标为双列卡片，筛选与列表可继续纵向访问；全屏档案保持单内容滚动和固定底部动作，接入证据完整可读；无页面级横向溢出。
5. 隐私：页面和响应仅展示连接/凭证状态，不展示 Cookie、Token、浏览器存储或其他敏感正文。

## 结论

- `V6-REAL-001`：passed（开发侧运行态）。
- `V6-REAL-002`：passed（开发侧运行态）。
- 本批 Product Design QA：passed；V6 长期目标和 Wave 4～8 仍未完成。

独立测试随后确认桌面、390×844、首载未知态、Escape 关闭回焦、实时只读 API、敏感字段扫描和控制台均通过；`V6-REAL-001/002` 无未决 P0/P1，候选可转正式版本。

final result: passed for v3.0.5 account fact consistency only

---

# Product Design QA — 客服路由账号隔离（v3.0.4 候选）

- QA 日期：2026-09-15（Asia/Shanghai）
- 环境：macOS 裸机应用 `http://127.0.0.1:3000`，Docker 仅复用 MySQL 5.7。
- 输入证据：独立测试 `G31-BUG-001`、V6 点击审计与鱼麦多参考信息架构；仅采纳上下文不丢失、范围可见和风险可执行原则，不复制品牌、文案或素材。

## 交互与视觉决策

- 客服首载不再短暂显示默认首店；路由账号授权确认前，页面只显示“正在载入客服账号范围”状态，并明确说明不会展示其他店铺数据。
- URL 中的账号、买家和页签是同一上下文；刷新、前进、后退以及快速切店均保留一致选中态。
- 无权限不再默认回退到其他店铺；显示单一、明确、可返回的状态卡，并隐藏全部业务页签和数据面板。

## 运行态检查

- 桌面：从账号 102 买家 `QA-BUYER-4` 进入客服，切换 AI 待接管后刷新，账号、页签、URL 和空态一致，无账号 101 / `QA Buyer -9` 闪现。
- 历史导航：通知消息↔AI 待接管前进后退正确；快速 101→102 切换后最终只显示 102。
- 无权限：`accountId=201` 保留原 URL，只显示无权限说明，不展示任何店铺选中态、买家或业务面板。
- 390×844：待接管和无权限两种状态完整可读，不渲染隐藏工作区，无页面级横向溢出；检查后已恢复默认视口。
- 控制台：最终部署后无新增 warning/error；重启窗口中已有的 Network Error 不属于稳定态缺陷。

## 结论与边界

- Product Design 前端开发：passed。
- 设计 QA（桌面、390 窄屏、加载、空态、无权限、快速切换）：passed。
- `G31-BUG-001` 已由独立测试按 `QA-G31-MSG-ACCOUNT-001` 回归并 Closed / Pass；Gate 3.1 当前无未决 P0/P1。

final result: passed; independent regression confirmed

---

# Product Design QA — Gate 3.1 第二批上下文与移动客服（v3.0.3 候选）

- QA 日期：2026-09-15（Asia/Shanghai）
- 环境：macOS 原生 Java/Vue `http://127.0.0.1:3000`，Docker 仅运行 MySQL 5.7。
- 参考方法：延续用户提供的鱼麦多截图所体现的“详情不丢上下文、风险可执行、信息有来源、移动端分层”，不复制其品牌、文案、图标或素材。
- 安全边界：Tenant-A、OWNER、账号 101/102/103 与 `QA-*` 数据；二维码只生成不扫码，消息只读不发送，不执行任何真实平台写入。

## 实机结论

1. 客服账号上下文：直接进入账号 102 后，买家会话、通知、AI 待接管切换及刷新都保留账号和 URL，不再回退到首店或显示“暂无可用账号”。
2. 手机客服：390×844 下只呈现当前阶段；从列表进入会话、再进入资料均有明确返回路径，消息输入和资料操作不再被三栏纵向堆叠淹没。
3. 买家跨页：客服工作台的买家 ID 可精确打开相同账号的买家全链路档案；目标档案展示 10 个订单、14 条消息和 10 个关联商品，未知金额仍为 `—`。
4. 订单跨页：订单 ID 深链自动过滤并打开唯一详情；同一对象的店铺、买家、金额、履约、退款和数据来源一致。协作旗帜 `NONE` 改为“无标记”，不再误显示“无退款”。
5. 二维码：已有账号续期展示账号 ID、备注、系统店铺 ID、生成/本地失效时间、倒计时和刷新；新增账号明确说明身份需扫码后识别。两处都提示平台可能提前失效，不承诺人为延长平台二维码有效期。
6. 空态与错误：买家页区分首载、真实空态、无数据错误和保留旧数据的刷新错误；可操作错误提供重试，不把请求失败显示为 0。

## 视口与状态覆盖

- 桌面：订单全屏详情、买家全屏详情、客服三栏、连接续期二维码通过。
- 窄屏：390×844 客服列表/会话/资料三阶段通过，无页面级横向滚动。
- 加载、空状态、错误：买家列表有独立文案和重试；刷新错误保留上次成功结果。
- 无权限：沿用后端账号范围和既有 OWNER/OPERATOR/SUPPORT 权限测试，本批没有通过前端伪造权限状态。
- 大数据：300 订单稳定筛选；买家档案关联 10 单/14 消息；此前 500 会话/通知和 1000 商品仍由既有服务端分页/任务夹具覆盖。

本批开发侧设计 QA 结论：passed。Gate 3.1 仍因 `V6-CLICK-002/003/010` 和独立测试回归保持 open。

最终 3.0.3 裸机部署后再次检查浏览器控制台，13:54 后无新增 warning/error；测试视口已恢复为默认桌面尺寸。

---

# Product Design QA — Gate 3.1 第一批入口恢复（v3.0.2 候选）

- QA 日期：2026-09-15（Asia/Shanghai）
- 运行环境：macOS 裸机 Java/Vue `http://127.0.0.1:3000`，Docker 仅复用 MySQL 5.7。
- 证据基线：当前系统逐功能点击档案、用户提供的鱼麦多店铺详情参考图；只采纳信息架构、上下文保持和可执行风险提示，不复制品牌、文案或素材。

## 本批运行态结果

1. 连接管理：账号卡是可键盘操作的按钮并暴露选中状态；选择账号 102 后，右侧身份、`?accountId=102` 和完整档案 `/connection/102` 一致。
2. 运营中心：使用向导、资源与规则、任务记录、分销结算切换真实正文和 URL；标准 tab 语义完整。素材页稳定显示 18 条真实 QA 资源，批量账号选择不再撑开工具栏。
3. 通知与诊断：五栏目切换、URL 和正文一致；异常空态有明确解释，刷新期间保留上次成功内容。
4. 系统设置：七栏目切换真实正文与 `panel` 深链；AI 未启用时展示不可用原因和当前配置，不误报正常。
5. 手机：390×844 下通知渠道卡完整；设置栏目改为单行横向导航，正文获得主要视口高度；运营中心主栏目、资源类型和宽表各自使用明确横向滚动边界。

## 状态、可访问性与边界

- 首载、刷新、真空态、错误和无权限由统一异步状态建模；旧请求不能覆盖用户新选择的页签。
- 运营/诊断栏目具备 `tablist/tab/aria-selected`；连接账号具备按钮和 `aria-pressed`；设置栏目为语义按钮并标记当前页。
- 桌面和 390 窄屏最终页面无新增控制台 warning/error，主操作可达；响应式测试结束后已恢复默认浏览器视口。
- 未点击批量发布、立即发布、删除、通知测试、配置保存、连接或其他外部/平台写动作。

## 结论

- 本批页面与设计 QA：passed。
- Gate 3.1：in progress；订单、客服、买家加载、团队权限、二维码身份时效和手机客服分层仍按 V6.2 基线继续。

final result: passed for v3.0.2 batch only; Gate 3.1 remains open

---

# Product Design QA — 全功能点击审计与 Wave 3 回归（v3.0.1 候选）

- QA 日期：2026-09-15（Asia/Shanghai）
- 参考证据：用户提供的鱼麦多店铺详情截图、当前系统逐菜单点击截图和运行态数据；仅转译信息架构与可用性原则，不复制品牌、文案、图标或素材。
- 环境：macOS 原生 Java/Vue 应用 `http://127.0.0.1:3000`，Docker 仅复用 MySQL 5.7 `127.0.0.1:13306`。
- 安全范围：Tenant 1、OWNER、隔离账号 101/102/103 与 `QA-*` 数据；不执行真实平台写入或外部发送。

## 点击审计基线

- 登录后按当前信息架构逐项进入一级页面、二级详情、主要页签与安全入口，等待稳定态后记录页面事实、依赖、风险和缺口。
- 共沉淀 35 条桌面证据、6 条响应式证据、4 个本轮 P1 缺陷和 12 个 Gate 3.1 后续缺口；证据表见 `XIANYU_MATRIX_V6_CURRENT_SYSTEM_CLICK_AUDIT_2026-09-15.md`。
- 需求文档新增 `V6-CLICK-REQ-01～12`，长期实施计划新增 Gate 3.1；“页面存在”不再等同于“功能可验收”。

## Wave 3 四项回归

1. 未知事实：买家订单金额和订单数量缺少同步证据时显示 `—`，并显示“暂无可核对金额”等覆盖说明，不出现虚假 `¥0.00`。
2. 可读时间：数字字符串 epoch 在买家消息列表中格式化为本地日期时间，不暴露原始长整数。
3. 黑名单闭环：买家 360 可查看客户黑名单状态、来源和时间，可保存、回读、解除，并可直达相同账号和买家的客服会话；会话侧状态同步投影。
4. 极窄高度：`640×360` 等效 200% 场景中商品 360 仍显示“基本信息 / SKU / 营销 / 数据罗盘 / 事件”栏目，内容独立滚动，底部动作保持可达；4 个真实 SKU 完整可读。

## 状态与细节

- 桌面稳定态、窄屏、空态、未知态、长 ID、真实多 SKU 和深链接已实机验证。
- 黑名单测试状态在验证结束后恢复，二次读取确认自动回复恢复“正常”，未遗留 `[买家]` 阻断原因。
- 解除会话黑名单只清理由会话产生的阻断，其他人工阻断不被覆盖；该边界由后端测试验证。
- 最终页面未发现本批新增的滚轮自动回顶、横向溢出或控制台错误。

## 结论与边界

- `W3-BUG-001～004`：passed。
- Gate 3.1：in progress；`V6-CLICK-001～012` 尚需按依赖顺序继续开发和独立验收，因此本报告不宣称全系统完成。

final result: passed for Wave 3 regression only; Gate 3.1 remains open

---

# Product Design QA — 统一全屏 360 与可读事件（v3.0.0 候选）

- QA 日期：2026-09-15（Asia/Shanghai）
- QA 环境：macOS 原生 Java 应用 `http://127.0.0.1:3000`，Docker 仅复用 MySQL 5.7 `127.0.0.1:13306`
- 视觉参考：`/tmp/codex-remote-attachments/01a06ce4-25a6-7660-90e5-fd32bbf02361/A649EEC7-2EF6-4971-80BC-8C8159DABA23/1-照片-1.jpg`
- 范围：商品、订单、买家、商品任务和统一审计 360 档案；详情直达、筛选/分页/滚动上下文、机器码分层、桌面/4K/手机响应式
- 安全边界：只读打开 Tenant 1 的 QA 账号 101/102/103、`QA-GOODS-*`、`QA-ORDER-*` 和持久化 QA 任务；未执行发布、改价、上下架、发货、退款、删除、申诉、消息发送、成员保存或外部通知

## 参考转译与全视图比较

- 只采纳参考图的成熟信息架构：居中大画布、身份头、横向详情栏目、单内容滚动和固定动作区；没有复制鱼麦多的品牌、文案、图标或素材。
- 商品、订单、买家、任务和审计详情由窄抽屉统一为最大 `1440×900` 的全屏工作区；4K 居中，不无限拉宽；小屏使用 `100vw × 100dvh`。
- 详情 URL 保存对象 ID 和当前页签；关闭后恢复原筛选、分页与滚动位置。审计页额外修复首载竞态，复制的 `accountId + logId` 链接可直接还原详情。
- 用户层只显示中文业务名称、结果和来源；原始枚举、请求参数与 JSON 放入默认折叠的“脱敏高级详情与机器码”。未知项使用“其他事件 / 状态待核对 / 其他来源”，不直接泄露机器码。

## 五项保真检查

1. 视觉层级：对象身份、状态事实、详情栏目、业务卡片和底部动作从上到下稳定；危险或未接入动作与普通读取动作明确分层。
2. 布局：1366×768 商品详情为 `1322×724`、边距 22px；3840×2160 订单详情为 `1440×900`、居中 `x=1200/y=630`；两者页面均无横向溢出。
3. 字体与密度：延续现有系统字体和 11–24px 层级；长 ID、错误原因和字段差异可换行，不挤压主要事实。
4. 色彩：沿用暖黄主色与中性背景；成功、风险、部分覆盖和未知均同时有中文文字，不只依赖颜色。
5. 细节与完成度：标题、关闭按钮、页签 ARIA、焦点陷阱、Escape 关闭、固定底栏、空态、加载态和折叠高级证据均已覆盖。

## 实际状态和数据量 QA

- 商品：`QA-GOODS-0864` 主档声明 4 个 SKU，详情真实展示 4 个规格；平台状态、固定卡密内容和卡密池编号均为可读文本，不再展示 `{deliveryContent}` 或 `[9101]`。
- 商品未知证据：`QA-GOODS-0999` 保持“声明 4 / 已验证 0”的未同步解释，不将缺少 SKU 子项显示为业务 0。
- 订单：300 条隔离订单、15 页分页可用；详情能力区过滤 `accountId` 等元数据，并将实物发货、物流跟踪、争议、备注、改价、收货提醒等能力名称中文化。
- 任务：持久任务详情展示成功、失败、结果未知子项，状态来自后端任务事实，不使用模拟进度。
- 审计：88+ 条日志分页，支持对象/请求 ID、模块、结果、操作者和时间筛选；CSV 导出使用独立导出请求 ID，不再误作查询过滤条件。
- 手机：390×844 下审计工作区宽 390px、无横向溢出；内容区可独立滚动，Escape 关闭后 URL 回到列表上下文。
- 加载、空态、错误、无权限：各工作区保留明确等待/空态/安全降级；权限边界由既有 OWNER/OPERATOR/SUPPORT 隔离测试覆盖，未为了截图修改真实角色。
- 控制台：最终页面运行无新增 error/warn。

## 迭代记录

1. 订单能力对象含 `accountId` 元数据，初版错误渲染成能力卡；改为只渲染包含状态对象的能力项，并补齐全部中文名称。
2. 商品履约模板和卡密池初版直出模板/数组格式；增加只读格式化，不改变原始履约数据。
3. 审计详情首载时日志 ID 存在字符串/数字差异并受异步加载时序影响；改为显式等待当前账号日志加载，再按规范化 ID 打开。
4. 商品罗盘缺样本天数曾显示 0；改为 `—`，继续遵守“未同步数据不显示为 0”。

final result: passed
