# Product Design QA — v3.3.0-rc.1 客服与自动回复

## 对照证据

- source visual truth: `/tmp/codex-remote-attachments/01a06ce4-25a6-7660-90e5-fd32bbf02361/A649EEC7-2EF6-4971-80BC-8C8159DABA23/1-照片-1.jpg`
- source pixels: 1280 × 741；截图为鱼麦多店铺详情，用于对照暖黄配色、白色分区、风险优先、详情不丢上下文和可执行状态，不作为客服/自动回复页面的逐像素稿。
- implementation URL: `http://127.0.0.1:3000`
- implementation screenshots: Codex In-app Browser tab 6 的本轮浏览器内联截图；工具后端未暴露可持久化文件路径。
- desktop capture: 1920 × 1080 CSS px，浏览器 viewport override 1920 × 1080，1× 等尺寸输出。
- mobile capture: 390 × 844 CSS px，浏览器 viewport override 390 × 844，1× 等尺寸输出。
- states: 客服大数据量三栏、发送结果未知、人工核对长表单、自动回复知识空态、策略测试结果、手机列表→会话→资料、加载态、无权限态、QA Failure State Shop、长页面滚动。
- focused regions: 客服会话头部风险动作、右侧发送回执核对卡、自动回复首次响应折叠卡、无发送策略证据卡、手机端风险入口与核对表单。

## Findings

最终复核没有未解决的 P0/P1/P2。

- 字体与排版：沿用系统中文字体栈；标题、正文、辅助说明和状态标签层级稳定。桌面三栏与手机单列均未出现重叠；长 ID 使用换行或截断，关键事件号在核对卡内可读。
- 间距与布局：桌面客服为列表/会话/资料三栏；手机为列表→会话→资料分层。白色卡片、细边框、克制圆角和低阴影与参考图的信息密度相符。首次回复与议价默认折叠，避免低频配置挤占首屏。
- 颜色与令牌：主操作采用闲鱼风格暖黄；危险、成功、信息和禁用状态使用语义色。发送结果未知同时使用文字、边框和颜色，不只依赖颜色表达。
- 图片质量：参考图品牌头像和平台素材未复制。QA 商品图片不可用时使用明确的“暂无商品图片/图片不可用”状态，没有伪造业务图片。
- 文案与内容：风险文案直接说明“先核对，禁止重发”；策略测试明确显示平台写入 0、AI 调用 0、买家发送 0；未同步数据使用“未同步/不可用”，没有显示成 0。
- 图标与控件：使用既有图标体系和原生语义控件；主要按钮、页签、下拉、输入框与开关对齐一致，手机点击目标可用。
- 状态与交互：浏览器实测加载、空态、无权限、大数据量、结果未知、策略演练和长表单滚动。核对动作必须先预检，确认按钮在预检前禁用；演练不会触达 AI 或闲鱼平台。
- 可访问性：页签包含 tab/tabpanel 关系；错误和状态使用 alert/status；按钮具有可读名称；手机端返回路径明确；键盘 Enter 可执行策略演练。

## Comparison history

1. 第一轮发现：
   - [P1] 桌面发送结果未知的核对卡位于右栏折叠线以下，核心风险动作首屏不可见。
   - [P2] 首次回复与议价表单占据自动回复首屏过多空间。
   - [P2] 自动回复仍残留偏蓝的旧视觉，与参考图暖黄业务工作台不一致。
   - 修复：增加右栏顶部 sticky 风险入口；首次回复卡改为状态摘要+按需展开；统一暖黄主色、白色分区和低阴影。
2. 第二轮发现：
   - [P1] 手机会话页把核对动作藏在“会话资料”中。
   - [P2] 原始消息先到、工作台投影稍后到时，详情预读会弹出“会话不存在”的全局误报。
   - 修复：手机会话头部增加红色“发送待核对”按钮并直达表单；先确认工作台记录存在再读详情，详情错误改为局部静默呈现，并增加异步 revision 防止旧请求覆盖新会话。
3. 最终复核：
   - 1920 × 1080：三栏、风险入口、回执证据、策略演练、大数据量与滚轮通过。
   - 390 × 844：列表→会话→核对资料、自动回复商品列表→详情、长表单与滚轮通过。
   - 旧 UNKNOWN 记录经 V55 回填后显示 `LEGACY-EVENT-*` 和 `LEGACY-ATTEMPT-*`，不再显示“未返回”。

## Open Questions

- 参考截图是店铺 360 详情，不是本批客服/自动回复的同状态设计稿，因此本次按视觉语言、信息架构与风险可执行原则对照，而非逐像素复刻。
- QA 环境中部分商品源图片失效，当前以真实错误态展示；待平台重新同步图片后应再做一次图片裁切与清晰度抽检，属于 P3 跟进项。
- 浏览器控制台历史中存在应用重部署窗口产生的 Network Error；最终服务恢复后未出现新的功能性前端错误。

## Implementation Checklist

- [x] 桌面三栏与手机分层导航
- [x] 发送结果未知首屏可见且禁止盲目重发
- [x] 人工核对预检、证据和禁用态
- [x] 首次回复低频配置折叠
- [x] 知识/策略页暖黄视觉统一
- [x] 空态、加载、无权限、大数据量与长页面滚动
- [x] 1920 × 1080 与 390 × 844 浏览器实测

final result: passed
