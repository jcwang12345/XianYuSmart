# Product Design QA — 订单售后与退货物流证据（v2.4.1）

- QA 日期：2026-09-14（Asia/Shanghai）
- QA 环境：`http://127.0.0.1:3000` / `http://localhost:3000`，Spring `qa` profile，MySQL 5.7.18
- 范围：ORD-04 退款/售后事实、ORD-05 退货物流证据、订单详情抽屉和“记录平台已确认运单”弹窗
- 安全边界：仅使用 Tenant 1、QA 账号 101/102/103、`QA-ORDER-*`；未调用闲鱼平台写接口，未执行发布、退款、删除或申诉

## 视觉真值与实现证据

- 来源视觉真值：同一 Chrome QA 页面改造前现场截图。1920×968 视口中，订单列表显示“退款处理中 1”，详情却只有“退款 0”空态，缺少售后类型、处理期限、平台下一步和退货物流。
- 最终实现截图：同一 Chrome QA 页面、同一 `QA-ORDER-0000` 数据完成后截图。1920×968 视口中显示退货退款卡、金额事实、退货进度、平台下一步、双期限、买家退回与卖家补发两条物流证据；390×844 视口中抽屉和弹窗均完整可操作。
- 像素说明：浏览器截图像素与 CSS 视口一致；桌面 1920×968，窄屏 390×844。浏览器能力未暴露额外设备缩放倍率，按 1 CSS px 对 1 截图 px 记录。
- 聚焦区域：退款详情卡、物流证据长卡片、二级运单弹窗、安全说明、底部动作区。

## 全视图比较

- 信息架构：售后事实位于订单详情的“退款”页签，不离开订单上下文；物流证据嵌套在对应售后记录下。
- 状态表达：未采集金额显示 `—`，退款状态存在但案例未同步时显示“退款 · 未同步”，没有把未知数据展示成 0。
- 风险表达：弹窗首屏明确说明“不会向闲鱼提交退货、换货或退款决定”；先预检确认范围，再勾选平台事实确认后保存。
- 响应式：桌面双列字段和居中弹窗；390×844 下卡片两列、期限单列、弹窗单列并使用内部滚动与固定底部动作区，无页面级横向溢出。
- 大数据量：300 条订单列表正常分页；售后卡可展示多条物流证据，长请求来源和时间字段未破坏布局。

## 细节检查

- 字体：沿用产品系统字体栈，标题、标签、说明和等宽运单号层级清晰。
- 间距：卡片 12–16px 节奏一致；桌面和窄屏的边距、字段间隔及固定操作区未遮挡正文。
- 色彩：延续闲鱼感的暖黄主色；处理中、运输中和只读提示有文字辅助，不只依赖颜色。
- 图片质量：本功能页无位图内容；图标使用现有矢量组件，无模糊缩放问题。
- 文案：平台事实、本地证据、未同步和安全降级边界均使用中文可执行文案；平台动作不再暴露原始枚举。

## 状态与交互 QA

- 默认/加载：刷新时保留骨架/等待反馈，数据返回后指标、列表和同步说明稳定落位。
- 空状态：`QA-ORDER-0091` 显示“退款状态尚未形成可核验案例”，并提供“重新读取订单事实”；不显示退款 0。
- 错误状态：只读角色请求得到 403 时保留未知指标和空表，不把失败结果当 0；页面同时显示只读权限说明和错误提示。
- 无权限：`matrixqa_support` 仅显示订单、消息、买家菜单；订单页顶部明确“修改、发送和执行操作已停用”，写操作入口不可用。
- 窄屏：390×844 验证订单页、全宽详情抽屉、页签横向滚动、退款卡和底部弹窗。
- 弹窗键盘：Escape 关闭二级弹窗后，焦点返回“记录平台已确认运单”触发按钮；关闭按钮有可访问名称。
- 两步确认：必填项不足时预检按钮禁用；预检返回的确认范围与平台写入“未执行”清晰可见；未确认平台事实前保存按钮禁用。
- 控制台：最终管理员页面未产生本功能新增错误。只读切换前记录到的两条“账号 Cookie 不可用”来自既有买家资料后台请求，不属于本次售后页面实现。

## 问题与修复历史

1. P2：平台下一步曾直接显示 `WAIT_BUYER_RETURN`，窄屏换行差。已改为“等待买家寄回”，保留 `platformActionCode` 供程序判断。
2. P2：卖家补发证据曾覆盖售后主档的买家退回进度。已限制只有 `BUYER_TO_SELLER` 方向更新退款主档状态；最终夹具保持“运输中”。
3. 复测结果：上述两项在桌面和 390×844 最终截图中均未复现。
4. P1 `XYM-ORD-001`：相同 `requestId` 改变载荷曾静默返回旧事实。已增加完整规范化载荷绑定校验；只有完全一致才能重放，任一字段或售后记录变化均返回 409。
5. P1 `XYM-ORD-002`：只读 SUPPORT 曾可调用 QA 售后夹具。已为 QA 控制面增加订单菜单、`action:order-write` 和账号范围三重校验。
6. P1 `XYM-ORD-004`：卖家补发物流事件曾覆盖买家退回主档文案。已与主进度同样按方向隔离，补发事件只显示在对应运单卡片。
7. 复测结果：本批无未决 P0/P1/P2。

## 构建与运行证据

- 无缓存前端类型检查：通过。
- 无缓存 Vite 生产构建：338 modules transformed，通过；最终产物重新写入 `src/main/resources/static`。
- 静态入口校验：`index.html` 引用的 JS/CSS 均存在。
- Maven 全量测试（Docker clean package）：141 tests，0 failures，0 errors，0 skipped。
- Flyway：MySQL 5.7.18 验证 39 个迁移，schema version 39。
- QA E2E：`safeFixture=true`、`platformNetworkCalls=false`、`platformWrite=NOT_PERFORMED`、幂等重放通过。

final result: passed
