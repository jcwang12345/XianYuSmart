# XianYuSmart 全功能点击审查与页面证据清单

版本：1.0
审查日期：2026-09-14
运行基线：`v2.6.1` / `db3d887` 加当前工作树
环境：macOS 裸机 Java/Vue，`http://localhost:3000`；MySQL 5.7 Docker `127.0.0.1:13306`
审查账号：隔离 QA 租户的 OWNER；店铺仅使用 101/102/103 测试夹具

## 1. 审查方法和边界

本轮不是根据源码猜功能，而是逐个进入路由、点击页签、打开详情或弹窗并观察真实返回。截图已在本次开发任务的浏览器证据流中按下列 `AUD-*` / `M-*` 编号留存。

- 桌面端：默认 1280×720 浏览器视口，覆盖所有公开路由、隐藏兼容路由和主要二/三级入口。
- 手机端：390×844，覆盖所有业务域的一级页面；检查横向溢出、核心动作可达、长列表/弹窗和加载状态。
- 安全边界：没有在生产店铺执行发布、改价、上下架、删除、发货、退款、申诉、通知发送、权限保存或密码重置。
- 可执行写链路只允许后续使用 `QA_LOCAL` / Mock 平台适配器，并必须保留请求 ID、幂等和审计。
- 页面中的“成功”“已同步”只有存在 API/数据库证据才算通过；静态按钮、占位页和前端模拟进度不算实现。

## 2. 页面与入口总表

| 证据 | 页面/入口 | 已点击内容 | 当前判断 | 主要缺口 |
|---|---|---|---|---|
| AUD-OV-01 | `/dashboard` 面板 | 时间窗口、范围、趋势、漏斗、排行、诊断入口 | 部分可用 | 与数据看板职责重叠；加载时整个指标区退化为 `—`，缺骨架和旧数据保留 |
| AUD-OV-02 | `/data-panel` 数据看板 | 指标卡、7天趋势 | 薄弱 | 与面板口径不一致；真实经营数据和覆盖说明不足 |
| AUD-OV-03 | `/command-center` 运营驾驶舱 | 账号总览、异常任务、客服队列、能力矩阵 | 部分可用 | 账号总览可能空白；能力矩阵出现系统异常；缺统一重试和定位 |
| AUD-AC-01 | `/accounts` 账号矩阵 | 新建分组、手动接入、扫码接入、360档案 | 部分可用 | 手动接入仍是旧式表单；列表读取期间显示全空；接入能力与原子性需端到端验收 |
| AUD-AC-02 | `/connection` 连接管理 | 选择店铺、凭证详情、扫码续期 | 部分可用 | 与账号矩阵状态不一致；扫码弹窗缺账号ID、备注、过期倒计时；技术语言过重 |
| AUD-AC-03 | 账号360档案 | 画像、经营指标、信用、处罚筛选、刷新 | 结构良好 | 风险数据覆盖/来源不够显眼；需补完整大数据、无权限、错误态 |
| AUD-PR-01 | `/goods` 商品矩阵 | 搜索/组合筛选、状态页签、批量入口、详情 | 部分可用 | 初始空态闪烁；手机按钮拥挤；来源/同步/统计口径在加载阶段不稳定 |
| AUD-PR-02 | `/product-publish` 商品发布 | 六段表单、草稿、预检、买家预览 | 开发中 | 真实平台通道仍需能力适配；高级字段不得静默丢失；草稿/预检/发布回读要完整E2E |
| AUD-PR-03 | `/product-tasks` 商品任务 | 任务列表、详情分栏、子项与时间线 | 后端基础已具备 | 手机首屏出现空白；需完成故障注入、恢复、通知和1000项大任务验收 |
| AUD-OP-01 | `/operations` 运营中心 | 使用向导、资源与规则、任务记录、分销结算 | 部分可用 | 信息架构宽而散；资源编辑使用原生多选，范围和风险提示不足 |
| AUD-OP-02 | `/opportunities` 商机发掘 | 搜索、采集、整理、配置、发布流程 | 部分可用 | 后续步骤依赖前一步但状态解释弱；外部来源与授权边界需明确 |
| AUD-OP-03 | `/price-comparison` 全站比价 | 搜索、价格区间、排序 | 基础可用 | 数据来源、抓取时间、样本范围和异常恢复不足 |
| AUD-OP-04 | `/supplies` 货源库 | 新增货源弹窗 | 部分可用 | 桌面720p弹窗过高且底部动作不可见；手机流程应改分步 |
| AUD-OP-05 | `/workflows` 工作流 | 保存、运行、节点流程 | 部分可用 | 手机仅呈现长表单，不适合画布；运行结果、版本和恢复入口不够显著 |
| AUD-OP-06 | `/automation` 隐藏页 | 风险事件、任务执行记录 | 兼容页 | 与运营中心/通知诊断重复，需合并或明确定位 |
| AUD-FU-01 | `/kami-config` 卡密仓库 | 新建仓库弹窗 | 部分可用 | 空态无引导闭环；弹窗对比度偏低；未展示库存池/SKU/消耗审计整体关系 |
| AUD-FU-02 | `/fixed-delivery-templates` 固定内容模板 | 新建模板弹窗 | 部分可用 | 模板与商品/SKU绑定链路不可见；缺预览、变量校验和引用影响范围 |
| AUD-FU-03 | `/auto-delivery` 自动发货 | 店铺筛选、规则/记录 | 不稳定 | 手机首屏空白；与卡密/模板/订单失败补偿分散，缺统一履约工作台 |
| AUD-FU-04 | `/orders` 订单与售后 | 概览、物流、退款、消息、时间线、备注 | 部分可用 | 退款只读降级合理但未完成平台决策；空消息/时间线过于空；事件码未业务化 |
| AUD-FU-05 | `/pending-orders` 隐藏兼容页 | 自动刷新、自动发货 | 应退场 | 与订单页重复且信息架构落后，应迁移功能后删除路由 |
| AUD-CS-01 | `/messages` 集成客服 | 买家会话、通知消息、搜索、人工接管 | 部分可用 | Cookie不可用时顶部报错但仍显示夹具；发送结果未知、防重复和跨店SLA需验收 |
| AUD-CS-02 | `/buyers` 买家管理 | 店铺/类型/关键词筛选 | 骨架可用 | 无真实买家夹具时无法验证订单、退款、会话、标签联动 |
| AUD-CS-03 | `/auto-reply` 自动回复 | 商品列表、规则入口 | 不稳定 | 手机长期加载；知识库、命中解释、效果评估和错误补偿入口不足 |
| AUD-SY-01 | `/operation-log` 操作日志 | 选择店铺、分页、详情 | 基础良好 | 首次DOM可访问性树为空；原始JSON可读性差；缺高级筛选/导出范围说明 |
| AUD-SY-02 | `/operations-health` 通知与诊断 | 系统检查、异常待办、站内通知、通知渠道、发送记录 | 部分可用 | 异常可处理但通知渠道/记录空态割裂；状态与业务对象跳转需统一 |
| AUD-SY-03 | `/settings` 系统设置 | 账号、菜单、AI、客服、邮件、备份、关于 | 部分可用 | 长表单独立滚动；未保存提醒、配置测试证据、权限和审计提示不足 |
| AUD-SY-04 | `/admin/users` 团队与权限 | 创建账号、权限设置、重置密码确认 | 基础良好 | 权限目录与实际菜单存在遗漏；创建弹窗过长；敏感动作缺搜索/权限模板/差异预览 |
| AUD-SY-05 | `/qrlogin` 扫码登录 | 前往账号、检查连接 | 仅桥接页 | 不是真实扫码流程，命名会误导；应合并到账号接入 |
| AUD-AUTH-01 | `/login` 登录 | 账号、密码、显示密码、租户注册 | 基础可用 | 2FA按后端挑战后再显示第三输入框的规则要固化；注册入口需明确商业/私有部署策略 |

## 3. 关键二/三级页面证据

### 3.1 账号与连接

- `AUD-AC-D01` 新建分组：入口可见；需验证重名、删除不删店铺、跨模块筛选同步。
- `AUD-AC-D02` 手动接入：字段和视觉仍是旧系统形态；接入失败是否回滚半成品账号需API/DB验证。
- `AUD-AC-D03` 扫码接入：点击后生成过程反馈不稳定，不能只显示“生成中”。
- `AUD-AC-D04` 360档案：居中近全屏、固定页头/页脚、左画像右风险，方向符合用户截图；不是抽屉。
- `AUD-AC-D05` 凭证详情：Cookie、WebSocket、H5 Token分开显示，但“未知/未设置/无效”在父子层级重复。
- `AUD-AC-D06` 扫码续期：二维码可生成并进入等待扫码；缺账号ID、账号备注、可读有效期和刷新策略。

### 3.2 商品与运营

- `AUD-PR-D01` 商品详情：基本信息、SKU、营销、数据罗盘、事件时间线五页签必须保持真实接口或明确降级。
- `AUD-PR-D02` 批量入口：无选择时给出提示，但应在选择后展示吸附范围条，并保留筛选快照。
- `AUD-PR-D03` 任务详情：主子任务、终态和局部结果可见；不能以模拟进度代替持久化状态机。
- `AUD-OP-D01` 运营资源：原生 `<select multiple>` 影响理解与触控，应换成可搜索多选、已选摘要和范围提示。
- `AUD-OP-D02` 新增货源：720p下底部按钮不可见；手机端应为分步全屏页。

### 3.3 订单与售后

- `AUD-FU-D01` 普通订单概览：金额、状态、履约、评价、本地备注和能力边界同屏。
- `AUD-FU-D02` 物流：没有平台事实时使用 `—`，没有伪造发货成功，符合基线。
- `AUD-FU-D03` 无退款：显示“当前证据范围无退款”，而非绝对“没有退款”。
- `AUD-FU-D04` 退款夹具：申请金额、平台下一步、双方截止时间、退货/补发物流和同步证据可见。
- `AUD-FU-D05` 时间线：真实事件存在，但 `RETURN_SHIPMENT_RECORDED` 等英文码应增加中文标题、操作者、请求ID和可展开字段差异。
- `AUD-FU-D06` 退款决策：当前明确标为不可用/只读，诚实但不算功能完成；后续只在可靠平台适配存在时开放动作。

### 3.4 系统与权限

- `AUD-SY-D01` 异常待办：30项发货人工核对可逐项处理；“全部标记已处理”必须先显示范围和不可逆影响。
- `AUD-SY-D02` 站内通知：处理、解决、忽略动作齐全；需分清已读和已处理。
- `AUD-SY-D03` 通知渠道/发送记录：空态存在，但缺引导、测试结果和脱敏错误样例。
- `AUD-SY-D04` 创建成员：菜单和功能权限分栏较完整；弹窗过长，底部固定但缺权限搜索与岗位差异摘要。
- `AUD-SY-D05` 权限设置：管理员只显示角色/状态，普通成员需验证账号范围、动态分组和敏感权限回读。
- `AUD-SY-D06` 重置密码：已检查到最终确认前，未提交；提示会清除登录状态是正确的。
- `AUD-SY-D07` 审计详情：已能展示字段级 before/after，原始请求/响应仍应折叠、格式化和脱敏。

## 4. 390×844 响应式证据

手机端截图使用 `M-<业务域>-<序号>`。全部路由的 `document.scrollWidth` 与 `innerWidth` 均为 390，没有页面级横向溢出，但“不溢出”不等于可用。

### 通过或基本可用

- `M-OV-01/02` 面板和数据看板：卡片单列，筛选可达。
- `M-AC-01` 账号矩阵：汇总卡两列、筛选下沉；首屏较长但可滚动。
- `M-PR-02` 商品发布：六段结构变为纵向，草稿和预检按钮可达。
- `M-OP-02/03/04/06` 商机、比价、货源、自动化：核心入口可达。
- `M-FU-01/02/04/05` 卡密、模板、订单、旧待发货：没有横向溢出。
- `M-CS-01/02` 集成客服、买家管理：双栏重排；消息夹具中的脚本文本以纯文本显示，没有执行。
- `M-SY-02/03/04/05` 通知、设置、团队、扫码桥接：能重排到单列。
- `M-AUTH-01` 登录：账号/密码正常；未启用2FA时第三输入框不显示。

### 阻塞问题

1. `M-AC-02` 连接管理首屏只显示标题和大面积空白，账号卡和详情加载不可感知。
2. `M-PR-03` 商品任务首屏只剩顶栏，任务列表/错误入口不可见。
3. `M-FU-03` 自动发货首屏空白。
4. `M-CS-03` 自动回复长期停在加载状态，缺超时和重试。
5. `M-SY-01` 操作日志在手机改成账号列表后缺明显“进入日志”的交互提示，信息架构发生跳变。
6. `M-OP-01` 运营中心页签右侧被裁切，只能横向滑动但无提示。
7. `M-PR-01` 商品页顶部四个动作两行拥挤，文字断裂；应收敛为主动作+更多。
8. 加载中的许多页面把统计值重置为 0 或 `—`，没有保留旧值/骨架/覆盖说明，容易误判。

## 5. 跨页面一致性缺陷

### P0

1. 连接状态在账号矩阵、连接管理、驾驶舱三处可能同时出现“已知”“未知”“未检测”，缺单一状态源和证据时间。
2. 二维码续期没有账号ID、备注和倒计时；多账号场景容易扫错账号。
3. 手机端连接、商品任务、自动发货出现空白/无恢复入口。
4. 订单退款决策和实物平台写能力仍未实现，只能保留安全降级，不能宣称完整。
5. 发布高级字段在真实通道不支持时必须预检阻断，不得保存后静默丢弃。
6. 所有加载态不得将未同步数据暂时显示为 0。

### P1

1. `/dashboard`、`/data-panel`、`/command-center` 三个概览入口职责和指标口径不清。
2. `/orders` 与隐藏 `/pending-orders`、`/operations` 与隐藏 `/automation` 重复，应合并能力并退场旧路由。
3. 空态页面只说“暂无”，缺少下一步、数据来源、需要的权限和安全影响。
4. 对话框/弹窗尺寸、标题、关闭、底部动作和内部滚动策略不统一。
5. 站内通知、异常待办、外部发送记录分别维护状态，缺统一事件身份和跳转。
6. 审计和事件时间线仍混有英文技术码和大段原始JSON。
7. 长表单没有自动保存、未保存提醒、分区错误导航和离开确认。

### P2

1. 四类仪表盘卡片、状态标签和主按钮的密度不统一。
2. 大屏没有最大阅读宽度/可配置密度验证；4K应避免内容无限拉伸。
3. 数据表手机端需要卡片/关键列模式，不应仅靠横向滚动。

## 6. 状态覆盖矩阵

| 状态 | 当前证据 | 结论 |
|---|---|---|
| 正常有数据 | 面板、驾驶舱、商品、订单、客服、审计 | 已覆盖，但跨页口径不一致 |
| 空数据 | 卡密、模板、货源、买家、通知渠道 | 已覆盖，指导性普遍不足 |
| 加载 | 账号、连接、任务、自动发货、自动回复、设置 | 已覆盖，存在0/空白闪烁和长期加载 |
| 错误 | 能力矩阵系统异常、消息Cookie不可用 | 已覆盖，缺统一错误详情和重试语义 |
| 无权限 | 路由守卫与团队角色存在 | 页面级证据不足，需专用只读账号逐路由复测 |
| 部分同步 | 面板、订单、商品 | 有来源/覆盖基础，仍需统一组件 |
| 大数据 | 1007商品、300订单、30异常、长退款物流 | 已有夹具；需1000任务/50SKU/长文本性能专项 |
| 高风险确认 | 删除/退款/权限/密码只检查到确认前 | 必须在QA Mock补齐幂等、审计和结果未知 |

## 7. 审查结论

系统已不是简单自动发货工具，已经形成账号、商品、订单、客服、运营、通知、权限和审计的产品骨架。但目前主要问题是“模块多、闭环不齐、状态语义不统一”：同一业务事实跨页表达不同，部分页面只有入口或空态，手机端仍有不可用页面，真实平台能力与本地能力的边界还需逐项固化。

后续开发以 `XIANYU_MATRIX_V3_PRODUCT_REQUIREMENTS.md` 为唯一详细需求基准，以 `XIANYU_MATRIX_LONG_TERM_EXECUTION_PLAN.md` 为实施顺序；原有需求编号与专项验收文件继续作为可追溯验收合同，不被覆盖或删除。

## 8. 2026-09-15 当前实现复核

本文件第 2～6 节保留首次盘点时的历史事实，不回写成“当时已通过”。后续 Product Design 复核及修复证据见 `XIANYU_MATRIX_PRODUCT_DESIGN_AUDIT_2026-09-15.md`：

- `M-AC-02` 连接管理空白首屏已修复为可感知加载，手机连接详情按钮溢出和错误红色语义已修复。
- `M-PR-03` 商品任务冷路由已复测，加载提示消失后筛选、任务卡和结果状态可见。
- `M-FU-03` 自动发货冷路由已复测，商品列表和二级配置页可见。
- `M-CS-03` 自动回复冷路由已复测，商品列表和二级配置页可见。
- 自动发货/自动回复的无效商品图已改用共享中性占位；当前修复仅关闭“空白/破图”问题，不代表履约和 AI 业务闭环的其余需求均已完成。
