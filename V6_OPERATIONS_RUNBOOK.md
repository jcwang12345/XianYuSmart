# V6 部署、备份、升级与回滚手册

本手册覆盖两种受支持的运行方式。开始前先确认目标环境、端口、数据库卷和备份目录，禁止把 QA 命令指向生产数据卷。

## 模式 A：macOS 裸机应用 + Docker MySQL

适合本机开发和隔离验收。Java 21、Node 22、前端构建、Maven 测试及应用进程运行在 macOS；MySQL 5.7.18 单独运行在 Docker。

### 运行与验证

```bash
scripts/native-qa.sh status
scripts/local-verify.sh
scripts/native-qa.sh deploy
```

- 应用：`http://127.0.0.1:3000`
- MySQL：仅监听 `127.0.0.1:13306`
- 应用日志：`.tools/native-qa/app.log`
- 不可变应用包：`.tools/native-qa/releases/`

`deploy` 会依次执行类型检查、前端生产构建和后端打包，保存旧应用包，原子切换新应用包并等待健康检查。新版本启动失败时会自动切回旧应用包。

### 数据库备份

1. 在“设置 → 备份与恢复”先导出业务备份并保存校验摘要。
2. 需要完整灾备时，对 `xianyusmart-native-qa-mysql` 执行 MySQL 逻辑备份；备份文件放在受访问控制且加密的目录。
3. 单独安全保存运行环境变量与加密密钥。业务导出不会包含 Cookie、Token、AI Key、SMTP 密码等秘密。
4. 抽样验证备份可解析，并记录创建时间、数据库版本、应用提交和 SHA-256。

### 升级

1. 确认工作树、目标提交和版本号，先执行 `scripts/local-verify.sh`。
2. 创建完整数据库备份，记录当前 `.tools/native-qa/app.jar` 指向的不可变包。
3. 执行 `scripts/native-qa.sh deploy`。
4. 检查健康接口、Flyway 迁移状态、登录、只读列表和一条隔离 QA 写入流程。
5. 不在真实店铺用发布、删除、退款或申诉验证升级。

### 回滚

- 新包健康检查失败：部署脚本自动切回上一不可变包。
- 新包已启动但业务验收失败：停止应用，把 `.tools/native-qa/app.jar` 原子链接切回记录的旧包，再启动并检查健康状态。
- 数据库迁移只前进不自动降级。涉及不兼容数据变化时，先停止应用写入，再从升级前完整备份恢复到隔离实例验证；确认范围后才恢复目标实例。
- `scripts/native-qa.sh stop-all` 只停止应用和 MySQL，不删除数据卷。

## 模式 B：全 Docker Compose

适合服务器部署。应用和 MySQL 都由仓库根目录 Compose 管理；公网访问应经 Nginx/HTTPS，不能直接暴露 MySQL。

### 首次运行与验证

```bash
cp .env.example .env
docker compose up -d --build
docker compose ps
docker compose logs --tail=200 app
docker compose logs --tail=200 mysql
```

必须在 `.env` 中设置独立的数据库密码、JWT 签名密钥和账号数据加密密钥。密钥不提交到 Git，不复制到工单、截图或日志。

### 数据库备份

1. 记录当前镜像摘要和应用提交。
2. 进入维护窗口，停止外部流量或应用写入。
3. 使用 MySQL 逻辑备份导出全部表、触发器和存储过程；保存退出码和 SHA-256。
4. 备份 Compose 文件、脱敏后的配置清单、证书位置说明和加密密钥的安全托管位置。
5. 在独立数据库实例完成恢复演练后，才把备份标记为可用。

### 升级

1. 固定目标 Git 标签或镜像摘要，不使用未经验证的浮动 `latest`。
2. 完成应用内业务备份和 MySQL 完整备份。
3. 拉取或构建目标版本，先在隔离环境执行类型检查、生产构建、后端测试和 Flyway 迁移验证。
4. 执行 `docker compose up -d --build`（固定镜像时使用 `pull` 后 `up -d --no-build`）。
5. 检查容器健康、迁移、登录、权限、诊断页和非破坏性业务回归。

### 回滚

1. 保留升级前的镜像摘要、Compose 配置和数据库备份。
2. 应用兼容旧数据库时，把镜像固定回旧摘要并重新创建应用容器。
3. 数据库不兼容时，停止所有应用写入，在隔离实例验证升级前备份，再恢复目标数据库并启动旧镜像。
4. 回滚后再次检查健康接口、权限即时失效、密钥只写响应和任务恢复状态，并记录原因与时间线。

## 通用验收与安全边界

- 健康检查里的“未知”不能当作正常；证据时间过旧时必须重新检查。
- 菜单排序只改变呈现，不改变后端权限。
- 停用成员、改变角色、权限或店铺范围后，旧会话的下一次请求必须被拒绝。
- 两步验证恢复码只显示一次；密钥读取接口只返回“是否已配置”和更新时间。
- 备份、恢复、升级和回滚都要记录请求 ID、操作者、版本、结果与审计证据。
- 任何生产写入验证都只允许操作明确标记、由本系统新建的测试对象。
