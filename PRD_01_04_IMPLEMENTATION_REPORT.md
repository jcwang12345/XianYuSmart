# PRD-01～PRD-04 商品模块实施与开发验收报告

日期：2026-09-14

版本：2.3.3

验收基线：`PRD_01_04_ACCEPTANCE.md`

环境：隔离 QA `http://127.0.0.1:12401/`，MySQL 5.7，Tenant-A，OWNER `matrixqa_admin`，安全店铺 101/102/103。生产端口 2000 未参与写操作验证。

## 结论

- PRD-01 独立测试指出的商品列表口径、状态、权限、上下文和响应式不足已修复；开发侧未发现未决 P0/P1。
- PRD-02 五页签全部连接服务端详情模型；平台无可靠适配器的营销/罗盘能力显示明确原因、来源和覆盖度，不展示假入口或假 0。
- PRD-03 本地资料编辑、复制 ID、同步/上架/下架/擦亮/删除的权限与状态入口已接入；改价、库存和未适配营销写能力明确禁用并标记“未接入”，不宣称平台修改成功。
- PRD-04 A～E 已由持久化父子任务、幂等键、执行期授权复核、按店限速、退避、部分成功、UNKNOWN、恢复、取消、失败重试、导出、通知、时间线和审计实现。进度来自数据库状态机，不是前端模拟。
- 独立 QA 尚需按本报告重新执行并签字；本报告只代表开发验证完成。

## 独立测试缺陷回归：XYM-PRD-001

- 根因：隔离 QA 主档按公式生成 `sku_count`，但未生成 `xianyu_goods_sku` 子项；详情接口此前也未单独表达 SKU 证据完整度。
- 修复：详情新增 `skuEvidence`，并在 `basic` 回写 `declaredSkuCount/verifiedSkuCount/skuCoverageStatus/skuCoverageMessage`。声明数与子项不一致时为 `PARTIAL/UNSYNCED`，只有商品覆盖为 `FULL` 且两者均为 0 时才是 `EMPTY_VERIFIED`。
- 隔离夹具：`QA-GOODS-0864` 为 4 个真实 SKU 和 4 个履约映射；`QA-GOODS-0960` 为 50 个真实 SKU 和 50 个履约映射；`QA-GOODS-0000` 为已确认无规格对照。
- 数据迁移：无生产迁移；夹具位于 `qa/fixtures/XYM-PRD-001-multi-sku.sql`，禁止在生产库执行。

## 独立测试缺陷回归：XYM-PRD-002

- 根因：本地资料编辑事件只写入能力模式，统一审计只记录提交值和更新后值，无法还原字段变化；自动化配置事件也只有更新后快照。隔离回归另发现首次创建商品自动化配置时未提供 MySQL 5.7 必填的自动评价内容。
- 修复：本地资料和自动化配置统一生成仅包含变化字段的 `before/after/fieldDiff`；事件与统一审计同步持久化可读字段名、旧值、新值、操作者、账号、请求 ID、结果和来源。首次保存自动化配置使用既有默认评价文案，重复保存不会覆盖商家自定义文案。
- 前端：商品事件页和操作日志详情均以红色旧值、箭头和绿色新值展示字段差异；空值显示“（空）”，无变化明确显示“本次保存未产生字段变化”。390×844 下差异卡单列重排，详情底部十个动作改为三列完整可达，不再依赖横向操作条。
- 隔离回归：`qa-reg-002-local-final-v232`、`qa-reg-002-automation-v232` 均返回 3 个准确变化字段，统一审计各命中 1 条；恢复请求为 `qa-reg-002-local-final-restore-v232`、`qa-reg-002-automation-restore-v232`，商品标题/支持政策/所在地与五个自动化开关均已恢复。
- 数据迁移：无；沿用 V35 schema，Flyway 校验 35 个迁移通过。

## 独立测试缺陷回归：XYM-PRD-003

- 根因：旧版本首次创建 `xianyu_goods_config` 时遗漏 MySQL 5.7 必填的 `xianyu_auto_rate_content`；补齐必填字段后，相同 requestId 重放仍可能再次更新配置和写入统一审计。并发回归还发现 REPEATABLE READ 事务快照可能让重放响应短暂返回赢家提交前的开关值。
- 修复：以商品事件唯一键原子预留自动化命令；只有预留成功者更新配置、事件和统一审计。重放通过 `SELECT ... FOR UPDATE` 读取赢家已提交事件快照，不重复写入；同 requestId 携带不同配置返回 409。首次创建继续写入系统默认评价文案，已有商家文案不被覆盖。
- 隔离回归：`qa-reg-003-auto-idem-v233` 覆盖无配置首次创建与顺序重放，`qa-reg-003-auto-update-v233` 覆盖已有配置更新，`qa-reg-003-auto-concurrent-final-v233` 覆盖两请求并发；每个业务 requestId 均只有 1 条商品事件和 1 条统一审计，重放响应均标记 `idempotentReplay=true` 且返回赢家状态。测试后五个开关已恢复为全关闭。
- 数据迁移：无；沿用 V35 schema，必填列由应用写入既有默认值。

## PRD-01 用例证据

| 用例 | 开发验证结果与证据 |
|---|---|
| P01-01 | 通过：`/products/query` 返回服务端生命周期聚合；QA 1000 件按在售/已售/下架/其他各 250，分栏和列表总数一致。 |
| P01-02 | 通过：商品 ID、标题、outerId 统一后端检索；空结果与接口错误使用不同 UI 状态。 |
| P01-03 | 通过：店铺/分组/状态/来源/通道/窗口进入服务端查询与分页快照。 |
| P01-04 | 通过：SELECTED 范围由 `AccountAccessService` 与数据权限 SQL 双层限制；越权 accountId 拒绝。 |
| P01-05 | 通过：所有 JDBC 查询显式 tenant_id；商品详情、事件、批次任务均复核租户和店铺范围。 |
| P01-06 | 通过：摘要标明全量汇总、1/7/30 窗口、数据日期、覆盖与最近同步。 |
| P01-07 | 通过：未同步经营数据为 null/UNSYNCED，界面显示“未同步/—”，不参与计算。 |
| P01-08 | 通过：商品图片使用固定尺寸占位，加载失败不影响选择和行高。 |
| P01-09 | 通过：慢请求保留旧列表并显示加载条。 |
| P01-10 | 通过：持久错误条与“重新加载”保留当前筛选上下文。 |
| P01-11 | 通过：隔离库 1000 件、20 件服务端分页，桌面与窄屏无持续冻结。 |
| P01-12 | 通过：Product Design QA 覆盖桌面、390×844 和浏览器缩放；核心动作可达。 |
| P01-13 | 通过：原生输入/选择/按钮语义，弹窗焦点陷阱、Esc 关闭和焦点返回。 |
| P01-14 | 通过：详情为覆盖层，不触发列表重建；关闭后筛选、页码、选择和滚动上下文保留。 |

## PRD-02 用例证据

| 用例 | 开发验证结果与证据 |
|---|---|
| P02-01 | 通过：无 SKU 返回明确空态，不构造虚假规格。 |
| P02-02 | 通过：多 SKU 显示规格、价格、库存、状态及自动发货映射；长表在抽屉内滚动。 |
| P02-03 | 通过：详情保留来源、通道、覆盖、同步时间和本地字段，不用本地值冒充平台真值。 |
| P02-04 | 通过：不可用能力直接禁用并给出通道/适配原因。 |
| P02-05 | 安全降级：粉丝价平台适配器不可验证，仅展示 UNSYNCED/不支持原因，无可提交入口。 |
| P02-06 | 安全降级：小刀/闲鱼币协议、余额和授权通道不可验证，不提供假入口。 |
| P02-07 | 通过：1/7/30 天窗口独立返回数据日期、覆盖和同步时间。 |
| P02-08 | 通过：商品动作事件包含请求 ID、任务 ID、操作者、结果和字段 diff。 |
| P02-09 | 通过：事件标记主动操作/Webhook/平台变化来源，event_key 唯一约束去重。 |
| P02-10 | 通过：原始快照端点独立授权并返回脱敏/摘要证据；普通角色不可见敏感原文。 |
| P02-11 | 通过：长标题、长错误和可滚动 SKU 区不破坏页头/底栏。 |
| P02-12 | 通过：加载、空、部分、失败、无权限分别表达。 |
| P02-13 | 通过：固定页头/底栏，主体独立滚动，Tab 循环、Esc 与关闭按钮恢复触发点。 |
| P02-14 | 通过：动作明确区分“读取缓存”“同步商品”，罗盘/营销同步按能力显示安全降级。 |

## PRD-03 用例证据

| 用例 | 开发验证结果与证据 |
|---|---|
| P03-01 | 安全降级：无已验证的平台商品编辑适配器；不开放平台编辑。 |
| P03-02 | 通过：仅本地编辑标题、支持政策、所在地，保存后回读并审计，文案明确不会修改闲鱼。 |
| P03-03 | 安全降级：改价/库存按钮禁用为“未接入”；后端预检仍执行值和版本校验。 |
| P03-04 | 通过：上下架非法状态在预检被拒绝；任务幂等防止重复副作用。 |
| P03-05 | 通过/安全降级：仅能力矩阵允许的商品可擦亮，限流可见；不可用时禁用。 |
| P03-06 | 通过：删除要求细粒度权限、影响范围、精确确认文案与审计；未对真实商品执行。 |
| P03-07 | 通过：平台超时进入 UNKNOWN，不自动重试，保留 platformRequestId。 |
| P03-08 | 通过：平台成功/本地失败可独立记录为 LOCAL_REPAIR_REQUIRED 并进入补偿语义。 |
| P03-09 | 通过：商品自动化配置保存后回读，人工接管/买家限制优先级由既有安全服务控制。 |
| P03-10 | 通过：前端能力状态与后端细粒度权限、店铺范围及运行状态同时校验。 |
| P03-11 | 通过：审计含旧值/新值、操作者、账号、请求 ID、平台/本地结果。 |
| P03-12 | 通过：幂等键、商品 row_version 和执行前条件复核处理双击/并发。 |
| P03-13 | 通过：错误保留上下文；只有确定失败子项可选重试，UNKNOWN 不可盲重试。 |
| P03-14 | 通过：任务结果写商品事件，列表/详情读取统一数据；通知携带批次 ID。 |

## PRD-04 用例证据

| 用例 | 开发验证结果与证据 |
|---|---|
| P04-01～03 | 通过：逐项/当前页/筛选快照选择，吸附条实时计数；跨页使用服务端筛选快照和排除 ID，筛选变化清空并提示。 |
| P04-04～05 | 通过：按 account_id 分组受控执行；预检返回权限、通道、状态、版本、授权冲突。QA 1000 件快照返回 3 店、1000 冲突的真实结果。 |
| P04-06 | 通过：tenant_id + idempotency_key 唯一约束；重复提交返回原任务。 |
| P04-07～09 | 通过：逐项成功/失败/UNKNOWN 保留；QA `PB-QA-UI-STATES` 展示三种终态，UNKNOWN 不可自动重试。 |
| P04-10～11 | 通过：启动恢复把不确定执行项转 UNKNOWN、可恢复任务重新排队；安全取消只停止未执行项。 |
| P04-12 | 通过：仅用户勾选且状态可重试的失败项生成重试，成功/UNKNOWN 不重复执行。 |
| P04-13～14 | 通过：每个子项执行前重新读取用户启用状态、权限、店铺范围和商品 row_version；撤权记 SKIPPED/AUTHORIZATION_REVOKED。 |
| P04-15 | 通过：删除预检强确认、独立权限和不可恢复提示；仅 Mock/单测验证。 |
| P04-16 | 通过：任务列表和详情按租户、账号范围裁剪并重新计算可见汇总，错误信息不越权泄漏。 |
| P04-17 | 通过：失败 CSV 脱敏导出并写 `PRODUCT_BATCH_EXPORT` 审计；QA 请求已有成功审计。 |
| P04-18～19 | 通过：成功/部分失败/失败进入站内通知和可配置外部 outbox；任务与逐件事件由 batch/request ID 关联。 |
| P04-20～21 | 通过：1000 件采用服务端分页与任务详情；刷新为用户触发/受控读取，桌面、390px、缩放和键盘可用。 |
| P04-22 | 通过：列表状态/计数、详情五页签、单品能力和事件仍读取统一商品事实；122 项相邻回归通过。 |

## 数据库、API 与可靠性

- 新迁移：`V35__product_batch_state_machine.sql`。新增商品 row_version/本地详情字段、父任务、子项、店铺限速状态及相关唯一键/索引。
- API：`POST /products/query`，商品详情/能力/本地编辑/自动化/事件/原始快照，筛选器 CRUD，经营导出，批次 preview/create/list/detail/retry/cancel/failures-export。
- 状态机：预检（待确认阶段）→ QUEUED → RUNNING → SUCCEEDED/PARTIAL_SUCCESS/FAILED/CANCELLED；子项另有 UNKNOWN/SKIPPED/LOCAL_REPAIR_REQUIRED。
- 取消策略：已成功结果不回滚；运行中请求不能伪装撤回；尚未执行项停止并重算汇总。
- 权限策略：创建时校验一次，每个子项执行前再次校验用户启用、动作权限和店铺范围；撤权不回滚历史成功结果。

## 最终构建与迁移验证

- `npm run type-check`：通过。
- `npm run build:spring`：Vite 341 模块生产构建通过，静态资源已同步到 `src/main/resources/static/`。
- `docker build -t xianyusmart:2.3.3 .`：通过；镜像摘要 `sha256:5a76fa79998199fcdba5de8f5b98dc8638e8bfd47df874cd3fdc2977ebae0b4e`。
- 镜像内 `./mvnw clean package`：122 项测试通过，0 失败，0 错误，0 跳过。
- 隔离升级库启动：Flyway 成功校验 35 个迁移，schema 当前版本 35，应用 health=healthy。

## Product Design QA

- 桌面商品矩阵：1000 条服务端分页、状态聚合、来源/覆盖/窗口口径清晰，无假 0。
- 390×844：筛选纵向排列；商品详情页头/五页签/主体稳定，底部十个操作以三列完整展示并保持可达；任务筛选、任务卡和详情可达。
- 空/加载/错误/无权限：均有独立表达和恢复入口；大数据不以前端一次性拼接 ID。
- 详情订单摘要在无证据时显示“当前缓存没有可验证订单；不把缺失数据显示为 0 笔成交”。
- `XYM-PRD-001` 桌面实测：4 SKU 为 4/4，50 SKU 为 50/50；价格、库存、平台状态与履约映射均在抽屉内可见，长表独立滚动且页头/底部动作不漂移。
- `XYM-PRD-001` 390×844 实测：声明/已验证提示优先显示，SKU 卡片单列重排，长表和底部动作分别横纵可滚动；截图检查未见溢出遮挡核心字段。截图只能证明可见布局，完整屏幕阅读器与键盘顺序仍由独立 QA 回归。
- `XYM-PRD-002` 桌面与 390×844 实测：商品事件和统一审计详情均可见标题、支持政策、所在地及自动化开关的旧值/新值；请求 ID、操作者、结果与来源未被折叠丢失。窄屏底栏调整后所有动作完整可达。
- `XYM-PRD-003` 桌面与 390×844 实测：商品详情“营销”页五个自动化开关、保存并回读和平台能力安全降级均完整可见；手机端保存成功提示明确，回读后开关保持一致，固定底栏未遮挡核心保存动作。截图可证明布局与可见反馈；键盘完整路径、屏幕阅读器播报及故障注入仍交由独立 QA 回归。

## 残余安全降级（非完成声明）

1. 闲鱼平台改价、库存和平台商品编辑适配器未得到可靠验证，入口保持禁用；只允许本地资料编辑。
2. 粉丝价、小刀、闲鱼币及营销/罗盘同步缺少可靠平台适配器，只展示能力限制与 UNSYNCED，不生成假数据。
3. `PENDING_CONFIRMATION` 为客户端/API 预检阶段；精确确认完成后才创建持久化 QUEUED 任务，数据库不会保存未确认的草稿任务。这是有意避免孤儿任务的产品语义。
4. 真实闲鱼平台回执、限流和 Webhook 不在生产账号验证；由 Mock、持久化夹具和单元测试覆盖，须由安全测试账号另行验收。
