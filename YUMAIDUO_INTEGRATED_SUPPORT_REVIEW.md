# 鱼麦多集成客服审查与 XianYuSmart 落地记录

审查日期：2026-09-13
审查范围：鱼麦多 `https://web.yumaiduo.com/console/im` 与 XianYuSmart `/messages`
审查方式：在用户已登录的 Chrome 会话中只读检查页面，并对本地前后端现有能力做代码核对。

## 结论

鱼麦多集成客服最值得继承的是“同一个工作台、两条明确分开的信息流”：买家会话用于沟通，通知消息用于处理业务事件。两者共享店铺上下文和刷新入口，但搜索、筛选、列表内容与空状态分别设计，因此不会把系统通知伪装成买家消息。

XianYuSmart 原客服页已经具备完整历史、图片消息、快捷回复、关联商品、买家档案和关联订单，业务处理深度不弱；主要缺口是通知仍分散在运营健康页。此次已将真实的异常待办与通知发送记录聚合到客服页，形成双收件箱。

## 鱼麦多页面证据

### 步骤 1：进入集成客服

- 页面地址：`/console/im`
- 状态：健康
- 观察：左侧一级导航为“集成客服”，页面顶部显示实时连接状态；工作区左侧有“买家会话”和“通知消息”两个同级标签。

### 步骤 2：检查买家会话

- 状态：健康
- 观察：包含店铺、联系人/商品/订单号搜索、只看未读、订单状态筛选、新消息提示音、刷新会话和更多功能。
- 空状态：明确告知店铺已连接、等待买家咨询，并解释浏览器声音和桌面通知提醒位置。
- 当前账号数据：无买家会话，因此未对消息详情、发送、订单联动进行写操作验证。

### 步骤 3：切换通知消息

- 状态：基本健康
- 观察：切换后搜索文案改为“搜索通知内容”，筛选改为“全部通知”，会话筛选不会错误保留。
- 当前账号数据：全部通知为 0，仅能确认信息架构、切换逻辑和空状态，无法确认真实通知详情、已读状态、分类或业务跳转。
- 体验问题：切换到通知空状态后，右侧主区域仍显示“还没有买家消息”，语义未随标签切换，是明显的空状态复用错误。

## 本地系统改造

实现文件：`vue-code/src/views/messages/workspace.vue`

### 双收件箱

- 页面标题从“在线消息”升级为“集成客服”。
- 新增“买家会话 / 通知消息”同级标签，并展示会话数量和待处理通知数量。
- 保留原有聊天发送、完整历史同步、图片、快捷回复、商品、自动回复、买家档案与订单入口。

### 真实通知聚合

通知消息由两个现有真实接口合并，不使用演示数据：

1. `/diagnostics/exceptions`：未处理的账号、订单、发货、商品、库存和客服 SLA 异常。
2. `/notifications/logs`：通过企业微信、钉钉、飞书、Bark、PushPlus、Telegram 或 Webhook 等渠道产生的发送记录。

两类数据在 UI 中明确区分：

- 红色“待处理业务通知”：可查看原因、所属账号、发生时间、跳转相关业务，并可标记已处理。
- 绿色“外部推送记录”：展示送达或失败结果，仅作发送审计，不提供误导性的已读操作。

### 检索与刷新

- 支持全部、待处理、发送记录三类筛选。
- 支持按标题、正文和事件类型搜索。
- 继承当前店铺上下文，同时保留全局工作台通知。
- 页面首次加载即静默获取通知数量；买家会话模式下每 10 秒同步会话和通知角标，通知模式下每 10 秒刷新通知列表。
- 通知事件按类型跳转到订单、商品、账号或运营健康页面。

### 空状态改进

本地通知空状态使用专属文案：“订单、退款、发货、账号安全和客服超时事件会集中显示在这里”，避免鱼麦多当前通知页仍提示“没有买家消息”的语义问题。

## 验证

- Vue TypeScript 检查：通过。
- Vite 生产构建：通过，365 个模块完成转换。
- `git diff --check`：通过。
- 鱼麦多页面仅进行了导航、标签切换和读取；未发送消息、未标记通知、未改变店铺配置。

## 后续数据能力建议

当前通知发送日志只有标题、渠道结果和错误信息，尚未保存完整通知正文；异常待办则有完整原因。若后续需要达到“通知中心审计”级别，建议为通知日志增加脱敏后的 `content_summary`、业务对象类型与业务对象 ID，并增加服务端已读时间和已读人，而不是只在浏览器本地维护红点。
