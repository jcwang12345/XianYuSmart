# V6 Wave 13 实施记录 — PRD-06 商品数据与事件证据

## 版本与范围

- requirements: `V6-PRD-06`，并相邻落实 `V6-BASE-01/02/05/10/12/14/15`、`V6-CLICK-REQ-05/06/07`。
- scope: 商品矩阵空结果、商品 360 的 1/7/30 天数据证据、商品事件时间线、中文状态和响应式操作区。
- safety: 只读检查租户 1、隔离账号 101 与 `QA-GOODS-0999/0000`；未创建商品写任务，未触达账号 202/203 或生产平台。
- migrations: 无。复用现有商品指标表、商品事件表和 V27 之后的数据结构。

## 后端交付

- `GET /api/product-matrix/products`：空结果返回结构化 `emptyState`，包含实际筛选范围、最后检查时间、原因和恢复动作；无商品时不把统计伪装成 0。
- `GET /api/product-matrix/accounts/{accountId}/products/{goodsId}`：数据窗口固定按 1 天、7 天、30 天返回；增加 `timelineState`，明确当前账号/商品范围、事件数量、最后检查和最新事件时间。
- 每个数据窗口返回 `scope/lastCheckedAt/source/sources/coverage/syncedAt/dataStartDate/dataDate`；每个指标字段均返回 `value/source/coverage/syncedAt/definition`。
- 没有样本时数值为 `null`、展示为 `—`；部分覆盖返回分子/分母。多个来源不会被判定为完整覆盖，避免把异口径聚合冒充完整事实。
- 商品事件增加服务端中文 `presentation`：标题、结果、来源、触发方式、摘要和下一步。原始事件码、请求 ID、平台回执、before/after、field diff 继续保留为高级证据。
- `UNKNOWN` 事件明确要求先核对平台结果，确认前禁止自动重试；失败、处理中、取消请求各自给出可执行下一步。
- 补齐 Wave 12 独立测试反馈：价格边界文案统一为“商品售价”；营销应用、QA 确认和 QA 夹具不再显示为未知码。

## 前端交付

- 商品 360“数据”页使用证据头、1/7/30 天窗口卡和字段级口径；未同步、部分覆盖、完整覆盖与真实 0 分开显示。
- 商品 360“事件”页默认显示中文业务摘要、操作者、来源、字段变化和下一步；机器码与原始 JSON 收入可展开高级证据。
- 商品列表无结果时展示实际组合筛选、最后检查、原因与恢复动作，不再只显示泛化空文案。
- 手机 390 宽底部操作区改为单行横向动作轨，避免多行固定按钮遮住正文；窄屏的数据证据、时间线和字段口径改为单列。
- 详情保持全屏档案模式而非抽屉；桌面、1080P、4K、390 手机和 640×360 高缩放等效窄屏均保持关闭、页签、正文和操作可达。

## Product Design QA

- evidence: 对照用户提供的鱼麦多店铺详情截图，只采用成熟的信息架构、详情不丢上下文、风险可执行和暖黄操作语言，没有复制品牌、文案、头像或素材。
- desktop: 1440×900 数据窗口按 1/7/30 天排序；事件页中文摘要、字段变化和下一步可扫描；滚轮下滑后异步内容不会抢回顶部。
- mobile: 390×844 的固定底部动作由四行压缩为单行动作轨，正文首屏恢复；数据与事件长内容均可滚动到达。
- 200%/4K: 640×360 等效视口中关闭、页签、正文和动作轨可达；3840×2160 使用受控最大宽度，无超宽拉伸或裁切。
- states: 加载过程显示骨架/`—`；未同步数据说明“不代表 0”；大数据列表实测 1013 件/51 页；事件未知态给出安全核对动作；最终浏览器控制台 warning/error 为 0。
- evidence limitation: Product Design 浏览器工具只返回会话内联截图，未提供可提交的 PNG 路径；URL、视口、状态和结论已记录在 `design-qa.md`。

## 验证结果

- targeted backend: `ProductMatrixServiceTest,ProductMarketingServiceTest` — 32/32 passed。
- full backend suite: 309 tests / 0 failures / 0 errors / 0 skipped；随后新增列表汇总范围回归，定向测试后总用例为 310。
- frontend type check: passed。
- production build: passed，365 modules transformed。
- static assets: 使用最终源码重新生成，未采用独立测试 13:55 产生的中间产物。
- runtime: 裸机 macOS Java，`http://127.0.0.1:3000`；Docker MySQL `127.0.0.1:13306`。
- runtime truth check: 部署后列表顶部从错误的“7 天 · 完整”纠正为“1/7 天 · 部分”；商品 `QA-GOODS-0000` 详情同时显示 1 天未同步、7/30 天部分覆盖，口径一致。

## 已知安全降级

- 平台经营指标适配器尚未验证时，数据页保持未同步并引导去闲鱼端核对，不生成经营结论。
- 不同平台来源的指标不会合并后宣称完整覆盖；需要先统一口径再进入完整状态。
- 原始事件码未知时显示“其他商品事件/状态待核对”，同时保留机器证据供排查，不编造业务含义。
