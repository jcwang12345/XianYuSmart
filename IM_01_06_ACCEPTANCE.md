# IM-01～06 集成客服与自动回复验收基线

版本：1.0
日期：2026-09-15
上位需求：`XIANYU_MATRIX_V3_PRODUCT_REQUIREMENTS.md`
证据来源：`XIANYU_MATRIX_FULL_UI_AUDIT_2026-09-14.md`、`XIANYU_MATRIX_PRODUCT_DESIGN_AUDIT_2026-09-15.md` 及 2026-09-15 集成客服桌面/窄屏现场截图

## 1. 本批目标

把“消息能显示、偶尔能自动回复”升级为可追踪、可接管、不会因网络波动重复回复的客服工作台。所有状态必须来自服务端事实；平台未确认时显示“结果未知”，不得显示成功，也不得自动重发。

## 2. 已确认现状

- 买家会话和业务通知已有双收件箱骨架；会话、商品、订单和买家入口已关联。
- 手工文本/图片发送已有 `requestId` 和发送尝试记录，回执超时可进入 `UNKNOWN`。
- AI 回复已有消息去重、租约、发送前标记和结果未知保护，但未命中、低置信、敏感和模型失败不会形成统一的人工接管任务。
- 会话右栏在小于 1180px 时被整体隐藏，导致商品、自动回复、买家和会话运营上下文不可达。
- 无消息菜单权限时页面只呈现“暂无可用账号”，缺少明确无权限和返回路径。

## 3. 统一状态定义

### 3.1 消息发送

- `QUEUED`：已建立本地发送尝试，尚未触达平台。
- `SENT`：平台明确确认发送成功。
- `FAILED`：平台明确失败或发送前本地校验失败，可在确认原因后重新创建请求。
- `UNKNOWN`：外部尝试已开始但未取得可靠回执；禁止自动重发，必须先核对会话。

### 3.2 AI 决策

- `AUTO_READY`：规则或 AI 已生成符合安全门禁的回复。
- `AUTO_SENT`：平台确认已发送。
- `HUMAN_REQUIRED`：未命中、低置信、敏感、买家要求人工、超时或结果未知，进入人工队列。
- `PAUSED_MANUAL`：人工已接管，自动回复暂停至明确时间。
- `SKIPPED`：黑名单、自动化关闭或买卖双方身份不合法等可解释跳过。

### 3.3 人工接管任务

- `OPEN`：待认领。
- `CLAIMED`：已由某成员认领。
- `RESOLVED`：已完成处理，记录处理说明和时间。
- `IGNORED`：经判断无需处理，必须填写原因。

## 4. 功能验收

### IM-01 双收件箱

| 用例 | 验收标准 |
|---|---|
| IM01-01 | 买家会话与业务通知使用独立页签、独立筛选和独立计数，不互相混入。 |
| IM01-02 | 店铺筛选贯穿两个收件箱；无权限店铺既不返回记录，也不泄露计数。 |
| IM01-03 | 通知为空时说明会进入此处的事件类型；读取失败时保留旧数据并提供重试。 |
| IM01-04 | 会话列表支持未读、置顶、关键词/买家/商品/订单搜索，并说明返回范围。 |

### IM-02 可靠消息

| 用例 | 验收标准 |
|---|---|
| IM02-01 | 文本和图片发送都要求唯一 `requestId`，同租户同请求重放不二次触达平台。 |
| IM02-02 | 两个并发的相同请求只允许一个建立外部尝试，另一个回读首次结果。 |
| IM02-03 | 回执超时保存为 `UNKNOWN`，界面提示先核对会话，禁止一键盲重试。 |
| IM02-04 | `SENT` 才写入已发送聊天记录；`FAILED/UNKNOWN` 不伪造成功消息。 |
| IM02-05 | 图片必须来自已上传的 HTTPS 地址；大小、数量和失败项可定位。 |
| IM02-06 | 商品卡能力未有可靠平台适配时明确显示“暂不支持”，不得展示假入口。 |

### IM-03 AI 转人工

| 用例 | 验收标准 |
|---|---|
| IM03-01 | 规则和 AI 都未生成内容时创建一条 `OPEN` 接管任务，不静默丢失询问。 |
| IM03-02 | 买家明确要求人工时，不生成自动回复，直接进入人工队列。 |
| IM03-03 | 命中敏感/高风险意图时转人工，任务显示原因和原始消息摘要。 |
| IM03-04 | RAG 低置信或无可靠资料时转人工；阈值和最高命中分可追踪。 |
| IM03-05 | AI 调用失败或超时进入人工队列，不向买家发送内部错误文案。 |
| IM03-06 | 平台回执未知时任务标记“先核对是否已发送”，禁止自动重试。 |
| IM03-07 | 同一回复记录只能生成一个接管任务；进程重启后状态和认领人不丢失。 |
| IM03-08 | 认领、解决、忽略都校验租户、店铺范围和动作权限，并写统一审计。 |

### IM-04 商品知识库

| 用例 | 验收标准 |
|---|---|
| IM04-01 | “安标检查与笔型”知识明确：每次购买有效期 7 天，从第一次激活开始。 |
| IM04-02 | 售后问题引导通过有效链接或二维码加入售后群，由群内处理。 |
| IM04-03 | 说明产品为基于 Office/WPS 的插件，安装教程含视频和使用指导。 |
| IM04-04 | 明确群内有人工客服指导；禁止承诺未配置的服务时间和效果。 |
| IM04-05 | 知识按商品、版本、生效时间管理；过期二维码不得继续作为可发送资料。 |

### IM-05 回复可解释

| 用例 | 验收标准 |
|---|---|
| IM05-01 | 每次决策记录策略类型、命中规则/关键词、RAG 文档与分值、意图和上下文范围。 |
| IM05-02 | 能获取时记录模型标识和总耗时；获取不到显示“未记录”，不得虚构。 |
| IM05-03 | 记录安全门禁结果、人工接管原因、发送状态、平台回执和错误摘要。 |
| IM05-04 | 会话详情能查看最近决策时间线；敏感正文按权限展示，审计不得明文记录密钥。 |

### IM-06 客服效率与响应式

| 用例 | 验收标准 |
|---|---|
| IM06-01 | 未读、首响 SLA、超时、置顶和关键词标记均来自服务端，不因刷新归零。 |
| IM06-02 | 1366/1440 桌面显示会话、正文、上下文三栏；窄屏和手机不隐藏上下文。 |
| IM06-03 | 小于 1180px 时上下文改为可展开区或详情页；商品、买家、订单、自动回复和会话运营均可达。 |
| IM06-04 | 无权限显示明确原因和返回入口；空、加载、失败、超时、长会话、100 会话均可辨识。 |
| IM06-05 | 消息正文独立滚动，页面滚轮不回弹；编辑器和发送按钮始终可达。 |
| IM06-06 | 键盘可完成切换会话、输入和发送；焦点可见，不能只靠颜色表达状态。 |

## 5. 安全测试边界

- 自动发送、人工发送和接管动作只允许 QA 本地夹具/Mock；不得向生产买家发送消息。
- 可读取生产形态页面和本地镜像，但不得以真实账号执行消息发送、发货、退款或申诉。
- 故障注入覆盖：明确失败、回执超时、AI 不可用、低置信、敏感、权限撤销和进程重启。
- 测试正文使用 `QA-` 前缀，不包含真实个人信息、Cookie、密钥或用户订单内容。

## 6. 本批交付门禁

1. V43+ 迁移在 MySQL 5.7 全新库和升级库通过。
2. 后端覆盖并发幂等、未知结果、人工任务去重、权限/跨租户、认领与解决。
3. 前端覆盖桌面、390×844、空、加载、错误、无权限、长会话和 100 会话。
4. 最终源码重新执行类型检查、生产构建和后端全量测试。
5. 向独立测试任务交接固定提交、运行方式、QA 夹具、故障注入和安全边界。
